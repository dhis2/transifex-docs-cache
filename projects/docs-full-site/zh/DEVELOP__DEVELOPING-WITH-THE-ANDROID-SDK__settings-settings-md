---
edit_url: "https://github.com/dhis2/dhis2-android-sdk/edit/master/docs/content/developer/settings.md" 
---
# 设定值  { #settings } 

 <!--DHIS2-SECTION-ID:settings-->

每次元数据同步都会下载设置。有多种设置：

```java
d2.settingModule()
```

- **系统设置**：系统范围内的属性，如 "flag "或 "style"。
- **用户设置**：用户特定的设置，例如`keyDbLocale`或`keyUiLocale`。
- **设置应用**：这些设置可提供对应用行为的更多控制。下一节将对此进行更多介绍。



## 设定应用程式 { #settings_app } 

 <!--DHIS2-SECTION-ID:settings_app-->

DHIS2实例可能包含一个名为“ Android设置”的Web应用程序，该应用程序可以远程控制应用程序中的某些参数。此应用程序的安装和配置是可选的。

该SDK在每次元数据同步中都会下载此配置，并将其保存在数据库中。 SDK会自动使用其中的一些参数（粗体）。

一般：

- 元数据/数据同步频率：该值必须由应用程序使用，并用于触发SDK中的同步。
- 移动配置：网关号，发件人号。它们必须由应用程序使用，并用于在SDK中配置SMS模块。
- **保留值**：要保留的属性值的数量。
- **加密数据库**：是否加密本地数据库。

**程序：**本节控制程序数据同步参数。它具有定义用于在所有程序的同步中使用的全局或默认参数的部分。另外，它允许为特定程序设置特定设置。所有这些参数都由SDK使用，并在同步过程中使用。

** DataSets：**本节控制聚合的数据同步参数。它有一个部分来定义在所有数据集同步中使用的全局或默认参数。另外，它允许为特定的数据集设置特定的设置。所有这些参数都由SDK使用，并在同步过程中使用。

```java
// General settings
d2.settingModule().generalSetting().get();

// Program settings
d2.settingModule().programSetting().get();

// DataSet settings
d2.settingModule().dataSetSetting().get();
```

尽管SDK会自动使用这些参数，但应用程序可能会在同步过程中覆盖其中的某些值。例如，它可能定义一个不同的TEI或事件限制或一个不同的下载策略（limitByOrgUnit，limitByProgram）。

