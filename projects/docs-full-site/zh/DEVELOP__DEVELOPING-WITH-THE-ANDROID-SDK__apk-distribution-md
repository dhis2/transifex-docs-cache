---
edit_url: "https://github.com/dhis2/dhis2-android-sdk/blob/master/docs/content/developer/apk-distribution.md"
revision_date: '2024-10-24'
tags:
- Develop
---

# APK 分发{ #android_sdk_apk_distribution }
网络应用程序 [APK 分发](https://apps.dhis2.org/app/dff273fc-909e-48af-b151-c4d7e9c8a12c) 用于确定 Android 应用程序必须使用的 APK。这是一种控制安卓应用程序版本的方法。

官方 Android Capture 应用程序会自动使用此参数，在发现新版本时提供更新。其他自定义应用程序可能会使用此参数来控制自己的更新。此逻辑必须在应用程序级别实现。

APK 分发应用程序允许按用户组定义不同的版本。SDK 将在内部处理这一逻辑，并确定当前用户必须使用的版本。

应用程序可通过使用 LatestAppVersion 版本库获取该版本：

```kt
d2.settingModule().latestAppVersion().get()
```

每次元数据同步都会更新该版本。要检查更新而不触发完整的元数据同步，可以使用这种方法：

```kt
d2.settingModule().latestAppVersion().download()
```

下载完成后，就可以像往常一样从数据库中读取版本。

有关该应用程序的更多信息，请查看 [docs](https://docs.dhis2.org/en/use/android-app/apk-distribution.html) 。

