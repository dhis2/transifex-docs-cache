---
edit_url: "https://github.com/dhis2/dhis2-docs/blob/master/src/user/data-administration.md"
revision_date: '2026年8月12日'
tags:
- Develop
- Use
---

# 资料管理 { #data_admin }

数据管理模块提供了一系列功能来确保
DHIS2数据库中存储的数据是完整的，并且
数据库性能得到优化。这些功能应在
由数据管理员定期检查以确保
存储的数据是最佳的。

## 数据完整性{ #data_admin_data_integrity }

DHIS2可以对数据执行各种数据完整性检查
包含在数据库中。识别和纠正数据完整性
问题对于确保用于
分析目的是否有效。每个数据完整性检查
将描述系统执行的操作以及一般步骤
可以解决这些问题。

在 **数据管理** 应用中，这些检查被划分为两个
选项卡：**标准检查** 和 **慢速检查**。 在大型数据库中，慢速检查可能需要
大量时间和资源才能完成（例如，
直接查询 `datavalues` 表的检查），因此
必须逐个运行，而不是一次性全部运行。 在 **标准
检查** 选项卡中，使用 **运行所有标准检查** 可一次性运行所有标准
检查。

### 没有数据集的数据元素 { #data-elements-without-data-set } 

每个数据元素必须分配给一个数据集。数据值
如果有数据，将无法将元素输入系统
元素未分配给数据集。选择
在主菜单中选择维护->数据库集->编辑，然后添加
将“孤立的”数据元素添加到适当的数据集。

### 没有组的数据元素 { #data-elements-without-groups } 

一些数据元素已经分配给几个数据元素组。
目前不允许这样做，因为这会导致重复
分析记录集中的链接数据记录提供了汇总
数据。转到维护->数据元素组以查看每个数据
确定元素并删除不正确的组分配。

### 违反排他组的数据元素 { #data-elements-violating-exclusive-group-sets } 

某些数据元素已被分配到多个数据元素组中，
而这些数据元素组属于同一个数据元素组集。DHIS2中的所有组集
均被定义为互斥的，这意味着一个数据元素
*只能*被分配到该组集内的*一个*数据元素组中。
请转至“维护”→“数据元素和指标”→“数据元素
组”，以审查完整性检查中识别出的每个数据元素。
要么将该数据元素从除其
应被分配到的组之外的所有组中移除，要么查看是否有某个组应被
移至另一个组集。

### 数据集中的数据元素，但不在表单或节中 { #data-elements-in-data-set-but-not-in-form-or-sections } 

数据元素已分配给数据集，但尚未分配
分配给数据集表格的任何部分。使用的所有数据集
部分表格，通常应在数据集中包含所有数据元素
恰好分配给数据集的一部分。

### 分配给具有不同期间类型的数据集的数据元素 { #data-elements-assigned-to-data-sets-with-different-period-types } 

数据元素不应分配给两个单独的数据集
期间类型不同。建议的方法是创建两个
单独的数据元素（例如，每月和每年的数据元素）
并将它们分配给各自的数据集。

### 数据集未分配给组织单位 { #data-sets-not-assigned-to-organisation-units } 

所有数据集应分配给至少一个组织单位。

### 类别组合无效的部分 { #sections-with-invalid-category-combinations } 

使用节表的数据集应仅具有一个类别
每个部分中的组合。违反可能是由于
将数据元素分配给节，然后更改类别
此数据元素在以后的某个时间点的组合。

### 具有相同公式的指标 { #indicators-with-identical-formulas } 

尽管此规则不会影响数据质量，但通常不会
具有两个定义完全相同的指标是有意义的。评论
确定的指标及其公式，并删除或修改任何
指示器似乎是重复的。

### 没有分组的指标 { #indicators-without-groups } 

所有数据元素和指标都必须分配到至少一个组中，
因此需要将这些指标分配到正确的数据元素
和指标组中。从主菜单进入“数据元素/指标”
→“指标组”，并将每个“孤立”指标
分配到正确的组中。

### 无效的指标分子 { #invalid-indicator-numerators } 

违反此规则的原因可能是错误地引用了
删除或修改的数据元素。查看指标并进行
分子定义的更正。

### 无效的指标分母 { #invalid-indicator-denominators } 

违反此规则的原因可能是错误地引用了
删除或修改的数据元素。查看指标并进行
分母定义的更正。

### 违反排他性组的指标 { #indicators-violating-exclusive-group-sets } 

某些指标已被分配到多个指标组中，而这些指标组
均属于同一指标组集。DHIS2 中的所有组集均
被定义为互斥的，这意味着一个指标*只能*
被分配到该组集内的*一个*指标组中。 请转至
“维护” → “数据元素和指标” → “指标组”，
以审查完整性检查中识别出的每个指标。您可以
将该指标从除应分配到的那个组以外的所有组中移除，
或者查看是否应将其中某个组
移至另一个指标组集。

### 重复期间 { #duplicate-periods } 

如果已从外部应用项目导入了期间，则可能是
某些时期可能会重复。如果您有任何时期
在这里似乎重复的，您将需要解决这些问题
直接在DHIS2数据库中。所有已分配给
重复的期间，应移至正确的期间，并且
重复期间应删除。

### 具有循环引用的组织单位 { #organisation-units-with-cyclic-references } 

组织单位不能同时是父级和子级，
直接或间接。如果发生这种情况，您将需要
直接在DHIS2数据库中解析循环引用
通过重新分配“组织单位”表中的“上级”字段
组织单位。

### 孤立的组织单位 { #orphaned-organisation-units } 

所有组织单位都必须位于组织单位
层级结构中。请转至“组织单位”→“层级操作”，并将
该组织单位移至层级结构中的正确位置。

### 没有团体的组织单位 { #organisation-units-without-groups } 

所有组织单位*必须*被分配到至少*一个*组中。该
问题可能是因为您根本未定义任何强制性的组织单位
组集，或者某些组织单位违反了强制性规则
。注意：如果您未定义强制性的组织单位
组集，则必须先通过“组织
单位”→“组织单位组集”进行定义，并至少定义一个强制性
组集（组集“类型”几乎适用于所有情况）。 如果您
已拥有相关组集，请转至“维护” → “组织单位组”以
审查每个已识别的组织单位，并添加相应的组分配。

### 组织单位违反必修课 { #organisation-units-violating-compulsory-group-sets } 

这些组织单位尚未被分配到任何一个
属于*必选*组织单位组集之内的组织单位组中。
当一个组集被定义为“必选”时，这意味着一个组织
单位必须被分配到该组集内的
至少一个组织单位组中。例如，所有组织单位都必须属于
“类型”组集中的某个组。它可以属于
\`医院\`、\`诊所\`或任何其他“类型”组——但必须
仅属于其中之一。请转至“组织单位”→“组织
单位组”，查看完整性
检查中标识出的每个组织单位。将所有组织单位分配到仅一个必选组中。

### 违反专属组的组织单位 { #organisation-units-violating-exclusive-group-sets } 

一些组织单元已分配给同一组织单元组集的多个组织单元组。所有组集都被定义为互斥的，意味着一个组织单元*只能*属于该组集中的*一个*组织单元组。请转到组织单元->组织单元组以查看完整性检查中识别出的每个组织单元。从除了应分配给它的组织单元之外的所有组中移除该组织单元。

### 没有组集的组织单位组 { #organisation-unit-groups-without-group-sets } 

此处列出的组织单位组尚未分配给
组集。转到维护->组织单位->组织单位
组设置并将组织单位组分配给适当的
组集。

### 无组验证规则 { #validation-rules-without-groups } 

所有验证规则必须分配给一个组。转到 **维护**
应用项目 > **验证规则组** 并将违规验证规则分配到一个组。
规则分配到一个组。

### 无效的验证规则左侧表达式 { #invalid-validation-rule-left-side-expressions } 

左侧验证规则定义中存在错误。请转至
**维护** 应用程序 -\> **验证规则**，然后点击
该错误规则旁的 **编辑**。点击 **左侧** 并进行必要的更正。

### 无效的验证规则右侧表达式 { #invalid-validation-rule-right-side-expressions } 

右侧验证规则的定义中存在错误。请转至
**维护** 应用 -\> **验证规则**，然后点击
有问题的规则旁的 **编辑**。点击 **右侧** 并进行必要的更正。

### 无条件的项目规则 { #programrules-with-no-condition } 

Report will highlight all the **Program rules** not configured with **Condition**. Evaluation for rules not having condition are always evaluated as false.

### 没有优先权的ProgramRules { #programrules-with-no-priority } 

Report will highlight all the **Program rules** not configured with **Priority**. This is optional but its existence is very important when **ProgramRuleActionType** is **ASSIGN**. Rules with ASSIGN action type should have higher priority then the rest of the action types.

### 不采取行动的ProgramRules { #programrules-with-no-action } 

Report will highlight all the **Program rules** not configured with any **ProgramRuleAction**.

### 没有dataElements的ProgramRuleVariables { #programrulevariables-without-dataelements } 

Report will highlight all the **Program rule variables** not configured with **DataElement**. Report will be based on source type configuration. DataElement should be provided when the source type of ProgramRuleVariable is **DataElement**.

### 没有属性的ProgramRuleVariables { #programrulevariables-without-attributes } 

Report will highlight all the **Program rule variables** not configured with **TrackedEntityAttribute**. Report will be based on source type configuration. TrackedEntityAttribute should be provided when the source type of ProgramRuleVariable is **Attribute**.

### 没有数据对象的ProgramRuleActions。 { #programruleactions-with-no-data-objects } 

报告将突出显示所有未配置任何数据对象的**项目规则操作**。数据对象可以是 **DataElement** 或 **TrackedEntityAttribute**。某些项目规则操作负责为数据元素或跟踪实体属性赋值。

### 没有通知的ProgramRuleActions { #programruleactions-with-no-notification } 

Report will highlight all the **Program rule actions** which have ProgramRuleActionType set to SENDMESSAGE/SCHEDULEMESSAGE where the configuration does not provide any link to notification.

### 没有部分ID的ProgramRuleActions { #programruleactions-with-no-section-id } 

Report will highlight all the **Program rule actions** which have ProgramRuleActionType set to **HIDESECTION** but configuration does not provide any section id.

### 没有项目阶段ID的ProgramRuleActions { #programruleactions-with-no-program-stage-id } 

Report will highlight all the **Program rule actions** which have ProgramRuleActionType set to **HIDEPROGRAMSTAGE** but configuration does not provide any program stage id.

### 无效的项目指示符表达式 { #invalid-program-indicator-expression } 

报告由无效** DataElement **或无效** TrackedEntityAttribute **引起的项目指示器表达式中的所有冲突。

### 无效的项目指示器过滤器表达式 { #invalid-program-indicator-filter-expression } 

报告由无效** DataElement **或无效** TrackedEntityAttribute **引起的项目指示器过滤器表达式中的所有冲突。

### 搜索配置未优化的受追踪实体属性{ #tracked-entity-attributes-having-a-suboptimal-search-configuration } 

报告指出，所有受监控实体的属性配置均未达到最佳状态，这可能会导致使用这些属性搜索受监控实体时，搜索性能变慢。

### 与被追踪实体属性{ #partial-trigram-indexes-on-trackedentityattributevalue-in-sync-with-the-tracked-entity-attribute } 同步的、针对trackedentityattributevalue的部分三画符索引 

报告了所有与数据库中三字节索引不一致的已跟踪实体属性。这意味着可能需要创建、删除某些索引，或者两者兼而有之。

### 已创建带有部分三字节索引的受追踪实体属性{ #tracked-entity-attributes-with-a-partial-trigram-index-created } 

报告了所有在 `trackedentityattributevalue` 表中具有部分三字节索引的被追踪实体属性。

## 保养 { #data_admin_maintenance }

表：数据管理应用项目中的数据维护功能

| 功能 | Parameter name |
|---|---|
| 清除分析表 | 彻底清空分析表。这些表用于为数据透视表、GIS 和报告生成汇总数据。 |
| 分析分析表 | 对所有分析表执行 PostgreSQL `analyze` 操作。 |
| 删除零数据值 | 从数据库中删除零数据值。对于使用聚合运算符“平均值”的数据元素，其注册的值不会被删除，因为在聚合数据时这些值具有重要意义，而对于使用聚合运算符“求和”的数据元素，其注册的值则会被删除。<br> <br> 减少数据值的数量将提高系统性能。 |
| 永久删除软删除的数据值/事件/注册信息/被追踪的实体实例 | 在 DHIS2 中删除数据值时，系统会将相应的数据库行标记为已删除，但不会实际删除该行。<br> <br> 运行此维护函数将从数据库中物理删除这些数据值行。 |
| 修剪时期 | 删除所有没有注册数据值的周期。减少周期数量将提高系统性能。 |
| 删除已过期的邀请 | 将删除那些代表已过期的用户账户邀请的用户。 |
| 删除 SQL 视图 | DHIS2 允许您将 SQL 视图作为系统对象进行配置和管理，并与其对应的数据库 SQL 视图相关联。<br> <br> 运行此维护功能将删除所有系统视图对应的底层 SQL 视图。请使用 **创建 SQL 视图** 功能来重新创建这些 SQL 视图。 |
| 创建 SQL 视图 | 重新创建数据库中的所有 SQL 视图。 |
| 更新类别选项组合 | 重建分类选项组合。在修改某个分类所属的分类选项后，可能需要执行此操作。 |
| 更新组织单位路径 | DHIS2 数据库中的组织单位表有一个名为“path”的列，其中包含每个组织单位在层级结构中所有上级单位的拼接字符串。<br> <br> 运行此维护函数将更新这些值，并确保其与当前的组织单位层级结构保持同步。该列由 DHIS2 管理，但在直接向数据库加载数据时，手动更新可能会有所帮助。 |
| 清除应用项目缓存 | 清除系统缓存。 |
| 重新加载应用 | 手动重新加载并检测已安装的 DHIS2 应用项目。<br> <br>系统启动时、安装或卸载应用项目时也会检测到已安装的应用项目。 |

## 资源表{ #data_admin_resource_tables }

资源表是在分析过程中使用的支持表
数据。通常，将这些表的内容与
从第三方应用项目（例如，
Microsoft Excel。分析模块也广泛使用它们
DHIS2。资源表的重新生成只能执行一次
解决了所有数据完整性问题。资源表也
每次运行分析过程时，
系统。

  - 组织单位结构 (analytics_rs_orgunitstructure)

    只要有任何情况，都应重新生成该表
    对组织单位层次结构所做的更改。这个桌子
    提供有关组织单位层次结构的信息。它有
    每个组织单位一行，每个组织一行
    单位级别以及以下所有父级的组织单位标识符
    作为价值的血统。

  - 数据元素组集结构 (analytics_rs_dataelementgroupsetstructure)

    该表提供有关哪些数据元素是
    数据元素组集的成员。该表有一行
    每个数据元素，每个数据元素组集合的一列，以及
    数据元素组的名称作为值。

  - 指标组设置结构 (analytics_rs_indicatorgroupsetstructure)

    下表提供有关哪些指标是成员的信息
    哪个指标组设置。该表各有一行
    指标，每个指标组集的一列，以及
    指标组作为值。

  - 组织单位组集结构
    (analytics_rs_organisationunitgroupsetstructure)

    下表提供有关哪些组织单位是
    组织单位组的成员。桌子有一排
    对于每个组织单位，每个组织单位一列
    组集和组织单位组的名称作为值。

  - 分类结构 (analytics_rs_categorystructure)

    该表提供有关哪些数据元素是
    成员属于哪个类别。该表的每个数据都有一行
    元素，每个类别一列，并且类别名称
    选项作为值。

  - 数据元素类别选项下拉列表名称 (analytics_rs_categoryoptioncomboname)

    只要有更改，就应该重新生成该表
    进行类别组合名称。它包含可读的名称
    用于类别的各种组合。

  - 数据元素结构 (analytics_rs_dataelementstructure)

    该表提供有关所有数据元素以及哪些数据的信息
    捕获数据的周期类型（频率）。期间类型为
    通过数据集成员身份确定，因此依赖于数据
    要成为具有类似周期类型的数据集的成员的元素
    明确的行为。

  - 日期时段结构 (analytics_rs_dateperiodstructure)

    该表提供有关所有期间以及哪个期间的信息
    与它们关联的类型。对于每个较低的期间类型
    频率比其本身，它包含有关其周期的信息
    将落入。

  - 期间结构 (analytics_rs_periodstructure)

    与 `analytics_rs_dateperiodstructure` 类似，包含一组受范围限制的
    更适合进行汇总分析的时间段。
    它是基于 `period` 和 `periodtype` 表的。

  - 数据元素类别选项组合
    (analytics_rs_dataelementcategoryoptioncombo)

    该表提供了数据元素与所有可能元素之间的映射
    类别选项组合。

  - 数据集组织单位类别 (analytics_rs_datasetorganisationunitcategory)

    本表提供了与数据集、组织单位相关的信息
    及其相关的类别选项组合。如果未找到关联，
    它使用默认的分类选项下拉列表。


## 分析表管理{ #data_admin_analytics_tables }

DHIS2 会生成数据库表，系统随后将这些表作为
各种分析功能的基础。如果您需要编写
高级 SQL 报表，这些表同样非常有用。在 **数据管理** 应用中，您可以立即执行
表的生成操作。 如果您希望安排系统定期执行这些操作，
可在**计划程序**应用中进行设置。这意味着您可以按需刷新
最新的分析结果，并查看更新的数据透视表，而无需等待过去所有年份的数据
重新处理完毕。

> **注**
>
> 您也可以通过网络应用项目接口生成表格。这项任务
> 通常由系统管理员执行。

1.  Open the **Data Administration** app and click **Analytics Tables**.

2.  选择要跳过的分析过程部分：

      - **跳过汇总数据和完整性数据的生成**

      - **跳过资源表的生成**

      - **跳过事件数据的生成**

      - **跳过注册数据的生成**

      - **跳过组织单位所有权数据的生成**

      - **跳过生成受追踪实体数据**（默认已勾选）

      - **跳过生成异常值**（默认已勾选）

3.  选择**要包含的最近几年数据数量**。（如果选择 0，
然后，您将运行最新或持续分析，具体定义见
[连续分析表](scheduling.html#scheduling_continuous_analytics_table)。)

4.  点击**开始导出**。

## 数据统计{ #data_admin__data_statistics }

数据统计模块提供了对象数量的概述
存储在DHIS2数据库中。


![](resources/images/maintenance/data_stats.png)

每种类型的对象的总数以一系列
表，其中包含每个对象的摘要统计信息。

## 锁异常{ #data_admin__lock_exception }

锁定例外允许对
被锁定的数据集进行细粒度的豁免控制。数据集过期后，默认情况下将
拒绝数据录入，除非已通过锁定
例外接口授予了例外权限。

### 添加锁异常{ #adding-a-lock-exception } 

通过授予锁定例外，即使在
数据集的有效期过后，仍可进行数据录入。

1. 打开**数据管理**应用项目，然后单击**锁定异常**。
2. 点击页面顶部的**“添加锁定例外”**
3. 选择所需的数据集、组织单位和时间段（参见
   （见下图）
4. 点击 **添加锁定例外**

![](resources/images/maintenance/create_lock_exception.png)

在上例中，系统会针对“Bo”的
“ART 月度汇总”数据集（“2014年2月”）触发一个数据锁定异常。

要运行此函数，无需额外权限。

### 删除锁定异常{ #deleting-a-lock-exception } 

1. 打开**数据管理**应用项目，然后单击**锁定异常**。
2. 在列表中，查找该组织单位、数据集的锁定异常
   以及您要删除的段落，然后点击其“删除”图标。
3. 点击 **“是，删除锁定例外”** 进行确认，或点击 **“否，取消”** 以
   留着吧。

这仅会解除该单一组织单位的锁定异常。

### 批量删除{ #batch-deletion } 

如果同一数据集和时间段的组合在多个
组织单位中均存在锁定异常（例如，同一数据集和时间段的锁定异常
影响了 100 个组织单位），您可以一次性
删除所有这些异常，而无需逐个删除。

1. 打开**数据管理**应用项目，然后单击**锁定异常**。
2. 点击页面顶部的**批量删除**。
3. 在列表中找到所需的数据集和时间段组合，然后点击
   其删除图标。
4. 点击 **“是，删除锁定例外”** 进行确认。

这将解除该数据集和时间段在*所有*
相关组织单位中的锁定异常，而不仅仅是一个。

## 最小-最大值生成{ #data_admin_min_max_value_generation }

此管理功能可用于生成最小值和最大值，
这些值将作为特定组织单位和数据集
数据质量与验证流程的一部分。只需从左侧框架中
选择数据集，然后从右侧的组织单位
选择器中选择需要生成最小值和最大值的
组织单位即可。 点击“生成”按钮可生成或
重新生成所有最小-最大值。点击“删除”可删除当前
存储在数据库中的所有最小-最大值。要运行此
功能，用户必须具备 `F_GENERATE_MIN_MAX_VALUES` 权限。

![](resources/images/maintenance/min_max_value_generation.png)

## 缓存统计信息{ #data_admin_cache_statistics }

此选项仅供系统管理员使用。缓存
统计信息显示应用项目级别缓存的状态。的
应用项目级缓存是指对象和查询结果
应用项目正在缓存以提高性能。如果数据库
已直接修改，需要清除应用项目缓存
它才能生效。

