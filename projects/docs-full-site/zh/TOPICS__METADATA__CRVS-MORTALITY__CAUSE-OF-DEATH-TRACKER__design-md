---
edit_url: "https://github.com/dhis2-metadata/CRVS_COD/blob/master/docs/crvs_cod-design.md"
revision_date: "2022-05-27"
---

# 世卫组织死因--跟踪和事件系统设计{ #crvs-cod-design }

## 总体设计{ #overall-design }

世卫组织死因跟踪系统的配置是为了有效收集和管理与死因有关的数据。该系统以国际死因医学证明和 ICD-10 为基础。

其设计包括两个程序，可根据收集数据的国家/组织的需要加以使用。

1. 死因（匿名）--无需登记的事件捕捉程序
2. 死因（跟踪器） - 注册程序

匿名程序是一个事件程序，而注册程序是一个跟踪程序。这两个程序共享相同的数据元素、自定义表单设计、程序指标、程序规则等。

这两个计划的比较如下，摘自实施指南

### 死因（匿名）--活动项目{ #cause-of-death-anonymous-event-program }

| 优点 | 缺点 |
| --- | --- |
| 结构/数据模式简单 | 无法以确保唯一性的方式记录识别信息，也无法对个人属性进行加密。 |
| 使用事件捕捉应用程序，该程序支持在网络浏览器中离线输入数据。 | 由于缺乏标识符，查找和编辑现有数据十分困难。 |
| 用户友好型数据输入屏幕，例如显示数据验证警告。 |

### 死因（登记）--追踪程序{ #cause-of-death-registration-tracker-program }

| 优点 | 缺点 |
| --- | --- |
| 支持使用唯一标识符和个人属性。例如，在考虑与其他民事登记和人口动态统计系统的互操作性以及查找和编辑数据时，这是必要的。 | 不支持在网络浏览器中离线输入数据。 |
| 当包含个人识别信息时，数据和系统会变得更加敏感。 | 数据输入屏幕的用户友好性较差，例如，与显示验证警告有关。 |

选择哪种版本取决于具体情况，不过，在许多情况下，活动程序的离线功能可能很重要。请注意，两个程序使用的变量（数据元素）是相同的，因此，如果从一个版本切换到另一个版本，两者的数据是可比的。

## 死因工作流程{ #cause-of-death-workflow }

死因模块使用基于 ICD（国际疾病分类）方法的框架来记录死因数据。在此过程中，[国际死因医学证明](http://apps.who.int/iris/handle/10665/40557) 是信息的主要来源。为了将信息输入 DHIS2，必须首先填写死因医学证明（这很可能是一个手动过程，而不是直接在 DHIS2 中填写）。然后，这些信息将通过已选择的程序类型之一输入 DHIS2。数据输入表本身就是纸质死因医学证明的复制品。有关该证明的更多信息/背景，请参阅《ICD SMoL 培训手册》。

工作流程](resources/images/CoD_Workflow.png)

## 计划概述{ #program-overview }

世卫组织死因模块的事件版和跟踪版都只包含一个程序阶段，因为它们在设计上非常相似；设计上的主要变化是在基于跟踪的程序中可以注册属性。死因模块使用自定义表单，以满足设计要求，在网络浏览器中密切反映纸质表单，但在使用移动设备的情况下，也做了相应调整。该结构如下图所示。

计划概述](resources/images/CoD_Program_overview.png)

下文将进一步详细介绍这些计划。

### 事件程序 - 死因（匿名）{ #event-program-cause-of-death-anonymous }

| 阶段 | 描述 |
| --- | --- |
| 死因（匿名） | 包含记录死亡原因医学证明书相关信息的所有必要信息。使用自定义表格布局，以反映纸质证书，其中的部分可在移动设备上使用。 |

### 跟踪程序 - 死因（登记）{ #tracker-program-cause-of-death-registration }

| 阶段 | 描述 |
| --- | --- |
| 注册/属性 | 登记包括一个单一属性，即系统生成的 ID。该 ID 可用于唯一识别死亡，并在需要时在系统中查找，但在大多数情况下会被现有的 ID 方案（如国民 ID）取代。它还可用作与其他系统（如民事登记）的链接。 |
| 死因（登记） | 包含记录医疗证明死因相关信息的所有必要信息。使用自定义表格布局来反映纸质证书，其中的部分可在移动设备上使用。这是一个不可重复的阶段；一旦登记了死亡详情，就不会再添加其他事件。 |

有关本设计中使用的计划、计划阶段和数据元素的完整列表和说明，请参阅完整的元数据参考资料。

## 计划规则{ #program-rules }

您可在此阅读有关计划规则的更多信息：

[配置程序规则](#webapi_nti_program_rules)

程序规则是跟踪程序和事件程序中死因模块的重要组成部分。程序规则不仅可以通过使用隐藏字段和显示错误/警告操作来减少数据输入错误，还可以使用 ICD-10 SMoL 和 ICD-10 完整代码来分配和编码基本死因；这被认为是该模块的一个关键功能，可以获得更高质量的死亡率数据。有关该程序规则的完整列表和说明，请参阅此处的详细元数据列表

### 隐藏场 { #hide-field }

隐藏字段操作用于使字段变灰/禁用（这是将隐藏规则操作应用于自定义表单；虽然由于自定义设计的原因无法隐藏字段，但可以使字段变灰，从而无法与之交互）。您可以在下面的示例中看到这一点

隐藏字段](resources/images/CoD_Date_of_birth_hidden.png)

由于出生日期未知，因此无法与出生日期进行交互；但出生日期不会被隐藏。

请注意，在移动设备上，该字段将被隐藏，因为表单将默认采用前面讨论过的部分设计。

### 显示警告/显示错误{ #show-warningshow-error }

表格中内置了许多错误/警告信息，以指出可能存在的数据输入问题。当事件程序检测到问题时，会立即显示这些信息

警告](resources/images/CoD_show_warning.png)

验证错误](resources/images/CoD_validation_error.png)

对于跟踪程序，它们只会在事件完成时显示。

错误/警告](resources/images/CoD_error_and_warning.png)

与会者讨论了显示这些错误/信息的差异，认为这可能是每种设计的优点/缺点，应在实施前加以考虑。

### 分配{ #assign }

指定规则操作用于确定以下 4 个数据元素的值：

-   年龄（岁
-   根本死因
-   ICD-10 SMoL 编码
-   ICD-10 完整代码

以年为单位的年龄值是根据出生日期和死亡日期的信息或估计年龄分配的，而后 3 个数据元素则是根据选择的基本死因分配的。有关选择死因的更多信息，可查阅世界卫生组织的 SMoL 培训资源：[https://www.who.int/healthinfo/civil_registration/smol/en/.](https://www.who.int/healthinfo/civil_registration/smol/en/.)

为了给 3 个数据元素分配不同的值，使用了 2 个选项集和一个包含多个部分的代码。在这种特殊情况下，确定死因的数据元素（例如，表格中的死因 C 被确定为根本原因）与选项集 "ICD SMoL - 本地字典 "相连。该选项集包含有可能在死因医学证明中被识别为死因 A、B、C 或 D 的不同因素的纯英文语言术语。该选项集还包含分为三个部分的每个选项的选项代码；这些代码用于为前面指出的不同数据元素赋值。

ICD SMOL](resources/images/CoD_ICD_SMOL.png)

请注意，代码之间用分隔符"|"分隔，以表示代码的不同部分。各部分的分隔情况如下

1. 代码 1：ICD-10 SMoL 代码
2. 代码 2：ICD-10 完整代码
3. 代码 3：用于唯一标识选项的代码

在 "ICD-SMoL-本地字典 "中使用的 SMoL 代码根据 ICD-10 SMoL 中的缩短列表确定最终的基本死因。从上例中可以看出，在 "ICD-SMoL-本地字典 "选项集中，可以有多个使用相同 SMoL 代码的选项，因为这些术语都与 SMoL 列表中用于统计的相同死因相对应。

在本例中，假设我们已确定急性肝坏死为基本死因（注意，这是为了解释设计，并不一定反映真实的例子），对应的 ICD-10 SMoL 代码为 5-74，ICD-10 完整代码为 K720。

ICD SMOL](resources/images/CoD_medical_data.png)

输入后

1. 它将采用选项集 "ICD SMoL - 本地字典"（5-74）中代码的第一部分，并将其分配给 "基本死因 "数据元素。该数据元素还与选项集 "ICD-SMoL "相关联。该选项集是可用于获取更高质量死因数据的死因简表。
2. ICD-SMoL "选项集中的代码 5-74 与 "其他消化系统疾病 "的值相对应。

   其他疾病](resources/images/CoD_other_diseases.png)

   我们可以看到，这将出现在屏幕底部为该数据元素指定的数据输入表单中

   结果](resources/images/CoD_results_underlying.png)

3. 它将获取选项集 "ICD SMoL - 本地字典"（5-74）中代码的第一部分，并将其分配给 "ICD-10 SMoL "数据元素。这只是一个纯文本数据元素，因此代码的显示与选项集中的内容完全一致。
4. 它将获取选项集 "ICD SMoL - 本地字典"（K720）中代码的第二部分，并将其分配给 "ICD-10 "数据元素。这只是一个纯文本数据元素，因此代码的显示与选项集中的内容完全一致。

结果](resources/images/CoD_results_complete.png)

这是用于在此表格中自动编码已确定的基本死因的一般流程，也是本模块的关键要求之一。通过使用这种方法，死因被正确编码，随后可根据需要进行汇总。

## 计划指标 { #program-indicators }

您可在此阅读有关计划指标的更多信息：

[配置程序指示器](l#configure_program_indicator)

与该计划相关的计划指标有很多。完整清单可在此处的详细元数据说明中查看：

计划指标用于该计划的典型指标：

1. 根据指定的 ICD-10 SMoL 代码将类似疾病归为一类
2. 按年龄组将死亡集中在一起

## 外部数据分析（ANACOD）{ #external-data-analysis-anacod }

死因程序获取的数据也可通过世卫组织的[死亡水平和死因分析（ANACOD）](https://www.who.int/healthinfo/anacod/en/)工具输出，供外部分析使用。ANACOD 电子工具提供了一种循序渐进的方法，使用户能够快速对有关死亡率水平和死因的数据进行全面分析。该工具可自动审查数据是否有误，将信息制成表格，以易于使用的表格和图表的形式显示结果，并提供将结果与其他国家组的结果进行比较的机会。

要将国家数据导出到 ANACOD 工具，您需要使用 DHIS2 中的 SQL 视图。SQL 视图包含在元数据包中，按年份分列（"CoD: ANACOD Export [YYYY]"）。要在 ANACOD 中按年份生成详细输出，需要为每一年新建 SQL 视图。由于新版本的 DHIS2 可能会更改为生成与 ANACOD 兼容的视图而访问的基础 SQL 表，因此您需要特别注意保持脚本的更新。SQL 视图将在 DHIS2 WHO demo 上保持更新，当您升级到新版本的 DHIS2 时，如果您的视图不再工作，可将其用作参考。

## 引用 { #references }

-   世卫组织启动死亡率清单（SMoL-ICD-10）：[https://www.who.int/healthinfo/civil_registration/smol/en/](https://www.who.int/healthinfo/civil_registration/smol/en/.)
-   世卫组织分析水平和死因（ANACOD）：[https://www.who.int/healthinfo/anacod/en/](https://www.who.int/healthinfo/anacod/en/)
