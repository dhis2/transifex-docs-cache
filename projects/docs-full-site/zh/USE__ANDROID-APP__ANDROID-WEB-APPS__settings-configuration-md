---
edit_url: "https://github.com/dhis2/dhis2-android-docs/blob/main/content/capture-app/app-android-settings-configuration.md"
revision_date: '2025-12-01'
tags:
- 使用
---

# 安卓设置网络应用项目{ #capture_app_android_settings_webapp }
## 概述{ #capture_app_android_settings_webapp_overview }

本节主要介绍 [Android 设置 Web 应用项目](https://apps.dhis2.org/app/a1bd6b5b-de8c-4998-8d34-56c18a139683) 的实现。


Android 设置 Web 应用项目](https://apps.dhis2.org/app/a1bd6b5b-de8c-4998-8d34-56c18a139683) 允许管理员配置 DHIS2 Android 应用项目的同步参数，加密 Android 设备的本地数据库，自定义项目、数据集和主屏的外观，以及添加 TEI 分析项目。在此 Web 应用项目中定义的配置参数将覆盖使用 DHIS2 Android 捕获应用项目的所有 Android 设备的设置。

请注意，在此版本的网络应用项目（2.3）中，如果 DHIS2 版本等于或高于 2.40，则只有拥有 "M_androidsettingsapp "或 "ALL "权限的用户才能在配置中定义这些参数。访问网络应用项目的其他用户可以看到参数值，但不能编辑它们。

> **警告**
>
> 如果 DHIS2 版本不高于 2.40，则只有拥有 "ALL "权限的用户才能在配置中定义这些参数。
> 


## 常规设置{ #capture_app_android_settings_webapp_general }

包括 Matomo URL 和项目 ID、每个 TEI 要下载的保留值数量以及加密设备数据库等配置。

![](resources/images/capture-app-general-settings.png)

### Matomo 配置{ #capture_app_android_settings_webapp_general_matomo }

DHIS2 Android 应用项目会发送匿名分析，可用于评估性能和/或早期发现问题。[Matomo](https://matomo.org)是用于此目的的工具，在特定服务器中运行，公众无法访问。不过，实施者可以建立自己的
 Matomo 实例（或使用云版本），以便收集和分析统计数据。 

Matomo 实例准备就绪后，您就可以得到如下图（左）所示的 URL 和项目 ID。ID 可以直接引入，但 URL 需要跟上 */matomo.php*，并以 *https* 或 *http* 开头，如下图（右）所示。

![](resources/images/capture-app-matomo-instance.png){width=33%}
![](resources/images/capture-app-matomo-settings.png){width=33%}


### 移动配置{ #capture_app_android_settings_webapp_generalmobile_configuration }
此部分允许管理员用户编辑短信网关广告结果发送方电话号码。
此配置可在 Android 应用项目中找到。
- 点击设置
- 点击短信设置

![](resources/images/capture-app-sms-settings.png){width=33%}

### 保留值{ #capture_app_android_settings_webapp_general_reserved_values }
这将指定每个 TEI 属性保留下载到设备中的值的数量。默认情况下，Android 应用项目保留 100 个 Ids，但也可以增加或减少这一数量。

要在 Android 应用项目中找到此配置，请执行以下操作
- 点击设置
- 单击保留值
- 要重新填充，请单击 *管理预留值*。

![](resources/images/capture-app-reserved-values.png){width=33%}
![](resources/images/capture-app-reserved-values-refill.png){width=33%}

### 加密设备数据库{ #capture_app_android_settings_webapp_general_encrypt_database }
这将迫使所有设备加密存储数据库，从而提高安全级别，防止数据被盗。

默认情况下，DHIS2 Android 应用项目数据库不加密，但管理员可以选中*加密设备数据库*，对存储在每个设备中的元数据和数据进行加密。加密数据库会影响安卓应用项目的数据库容量和性能（降低 10-15% 之间）。请注意，在选择或不选择该选项时，不会丢失任何数据（即使之前未与服务器同步也是如此）

> **警告**
>
> 这是一个关键操作，会影响所有与服务器同步的 Android 设备的本地数据库（不会影响服务器 DHIS2 数据库）。
>
> 默认情况下，安卓应用项目数据库不加密，但管理员可以选中*加密设备数据库*，对存储在每个设备中的元数据和数据进行加密。加密数据库会影响数据库容量和 Android 应用项目的性能。请注意，在选择或不选择该选项时，不会丢失任何数据（即使之前未与服务器同步也是如此）

### 允许截图{ #capture_app_android_settings_webapp_general_screenshots }
本部分允许配置使用 Android 捕捉应用项目截图或屏幕共享的可能性（使用 Teamviewer、Teams 等应用项目进行远程支持时需要使用该应用项目）。请注意，默认情况下屏幕截图是关闭的。

> **警告**
>
> 根据安全和隐私建议，DHIS2 Android 应用项目默认禁止截图（以及从其他应用项目捕获屏幕）。在某些情况下，允许截图可能有助于支持和/或共享目的，但在启用前请确保您了解此设置对安全和隐私的影响。
>


### 跳过 DHIS2 版本验证 (**v2.4**){ #capture_app_android_settings_webapp_general_skip_version_validation }
在连接 DHIS2 实例时，应用项目通常会验证与支持版本的兼容性。不过，在某些情况下，连接到不支持的实例可能是必要或有用的。请注意，默认行为是执行严格的版本验证。要跳过 DHIS2 版本兼容性验证过程，请选择复选框。

![](resources/images/capture-app-skip-version-validation.png)

> **警告**
>
> 此选项仅适用于使用 Android Capture 应用项目 3.0 或更高版本。
>


### 选择加入功能（**v2.3.1**）{ #capture_app_android_settings_webapp_general_optin_features }
选择启用功能是应用项目中的附加功能或设置，用户可根据喜好选择启用。与默认设置不同，这些功能需要用户的明确同意或操作（"选择加入"）才能激活。
选择加入允许用户通过向应用项目添加额外功能来定制体验。请记住，这些功能可能有特定的注意事项，因此建议在选择加入前查看任何相关信息。

要启用一项功能：

- 点击 *选项功能*。 
- 将显示可用的选择进入设置列表。
- 单击功能复选框启用该功能。

**数据输入表单（**v2.4**）：** 在 Android 捕捉应用项目中重新设计了所有值类型的输入，改进了选择模式，增加了可点击区域和文本，以提供更简洁、更直观的用户体验。默认情况下，Android Capture App 将显示新表单。管理员用户可单击复选框选项，选择不使用新版表单。

![](resources/images/capture-app-optin-features.png)

### 禁用所有设置{ #capture_app_android_settings_webapp_general_disable_settings }
单击此按钮，用户将删除所有 Android 配置设置。任何配置都不会应用到 Android 应用（如果是这种情况，应用的同步参数就是 Android 捕捉应用中定义的参数）。


## 同步{ #capture_app_android_settings_webapp_synchronization }

它提供了控制元数据/数据同步的附加参数。


### 全球{ #capture_app_android_settings_webapp_synchronization_global }

元数据同步
:   管理员用户可以选择元数据的同步频率，例如每 24 小时同步一次元数据。

数据同步
:   管理员用户可以选择数据同步的频率，例如每 6 小时同步一次数据。

![](resources/images/capture-app-sync-global.png){width=50%}
![](resources/images/capture-app-sync-global-app.png){width=33%}


使用新的跟踪器导入器
:   管理员用户可以选择使用哪个跟踪器导入端点。

> **警告**
>
> 该选项仅适用于从 2.38 版 DHIS2 API 开始使用的安卓设置网络应用项目。
>
> 默认情况下，如果 DHIS2 API 版本为 2.38 或更高，安卓应用项目将使用 2.37 */api/tracker* 端点中引入的导入器，但管理员可以取消选择此选项，并使用传统的跟踪器导入器 */api/trackedEntityInstances*。
> 
>![](resources/images/capture-app-sync-global-importer.png){width=50%}
>

使用新的 Tracker 输出项目（**v2.3**）
:   管理员用户可选择使用哪个跟踪器输出端点。

> **警告**
>
> 该选项仅适用于从 2.40 版 DHIS2 API 开始使用的安卓设置网络应用项目。 
> 
> 默认情况下，如果 DHIS2 API 版本为 2.40 或更高，安卓应用项目将使用 2.37 */api/tracker* 端点中引入的导出器，但管理员可以取消选择此选项，并使用传统的跟踪器导入器 */api/trackedEntityInstances*。
>
>
>![](resources/images/capture-app-sync-global-exporter.png){width=50%}
>

限制下载文件大小 (**v2.3**)
:   管理员用户可限制下载文件资源的最大大小（以 KB 为单位）。

![](resources/images/capture-app-sync-download-file-size.png){width=33%}

> **注**
>
> 管理员用户可以设置下载文件的最大资源大小。管理员现在可以不填任何值，以表示不限制最大文件大小。(**v2.4.1**) 
> - 无值：允许下载的文件资源大小不受限制。默认情况下，这是初始值。
> - 0：将下载限制为 0 KB。
> - 任何正值：限制下载的文件大小不得超过指定的最大限制（单位为 MB 或其他单位）。 
> 
> 这为管理员提供了灵活性，他们可以根据需要强制执行文件大小限制或允许不受限制的下载。
>



### 计划{ #capture_app_android_settings_webapp_synchronization_program }

本节控制节目数据同步参数。它有一个部分来定义要在所有项目的同步中使用的全局或默认参数。

#### 全局设置{ #capture_app_android_settings_webapp_synchronization_program_global }

全局设置适用于 Android 用户可以访问的所有项目。这些设置可以在全局、每个组织单位、每个项目或每个组织单位和项目中启用。

![](resources/images/capture-app-program-global-settings.png){width=50%}

* 要下载的 TEI：  从服务器下载 TEI 的最大数量。

* TEI 下载期限：  它下载在某段时间内更新过的 TEI，例如上个月更新过的 TEI。

* 要下载的事件：    要下载的事件的最大数量（从事件项目中下载，见下文注释）。

* 事件下载时间段：    它可下载事件日期属于特定时期的事件。

---
要检索数据审批工作流及其数据审批级别,您
可以发出类似这样的 GET 请求:

管理员可能会注意到，在为 TEI 和事件配置限制时，下载事件的总数似乎比预期的要高。这是由于 Android 应用项目应用这些限制的方式造成的：

* TEI 限制可控制下载跟踪实体的数量，包括其所有相关事件。
* 活动限制仅适用于活动项目（无需注册的项目）。
* 它不会限制属于跟踪项目的事件。

*示例：*

一个跟踪项目包含平均每个有 5 个事件的 TEI。如果 TEI 下载限制设置为 100，应用项目将下载这 100 个 TEI 及其所有相关事件（100 × 5 = 500 个事件）。
此外，一个事件项目包含 1,000 个独立事件，事件下载限制设置为 200。
安卓应用项目总共将下载

* 来自跟踪项目的 500 个事件（由于 100 个 TEI × 每个 5 个事件）
* 活动计划中的 200 项活动

总下载量达 700 次。

---

#### 特定设置（改进 3.3.0）{ #capture_app_android_settings_webapp_synchronization_program_specific }

项目特定设置允许管理员覆盖选定项目的全局同步规则。当不同的项目需要不同的限制或过滤标准时，这非常有用。

配置特定项目的设置：

1. 单击添加特定于项目的设置。

2. 在 "每个项目的值 "下选择一个项目。

3. 配置可用参数（因项目类型而异）。

**可用参数**

项目的具体设置因项目有注册（跟踪项目）或无注册（活动项目）而异。每个设置只有在适用时才会出现。

1. 无需注册的节目（活动节目）

参数可包括

* 设置级别 - 定义同步是否适用于所有机关单位或特定机关单位。
* 根据预定义列表视图下载事件（3.3.0 中的新功能）
* 最大活动下载量
* 下载事件日期在（可配置的时间范围内）的事件

2. 注册计划（跟踪计划）

参数可包括

* 设置水平
* 每个项目的最大 TEI 下载量
* 下载更新的 TEI
* 根据预定义列表视图下载 TEI（3.3.0 中的新功能）
* 下载带状态的 TEI
* 下载注册日期在以下范围内的 TEI

**基于列表视图的下载（3.3.0 中的新功能）**

当一个项目链接到一个或多个工作列表时，相应的过滤字段会有如下表现：

* Android 应用项目根据分配给用户的工作列表同步 TEI。
* 如果用户只能访问一个工作列表 → 则使用该列表。
* 如果用户可以访问多个列表 → 它们的内容会自动合并。

![](resources/images/capture-app-image363.png){width=50%}
![](resources/images/capture-app-image364.png){width=50%}

##### 查看和管理项目特定设置{ #viewing-and-managing-program-specific-settings } 

保存一个或多个特定项目配置后，这些配置将显示在汇总表中。
管理员可以从该表中

* 查看每个项目的当前配置
*编辑数值
* 删除项目特定设置

![](resources/images/capture-app-program-specific-table.png){width=50%}

##### 重要考虑因素{ #important-considerations } 

> **注意**
特定项目的设置可能会导致下载的 TEI 数量大于基于全局同步限制的预期数量。
出现这种情况是因为 Android 客户端分两步执行同步：
>
>1.全局同步：根据用户的组织单位和最后更新字段下载最多 X 个 TEI。
>2.特定项目同步：
为每个配置了特定设置的项目下载最多 Y 个额外的 TEI。
>
>如果全局同步中的 TEI 比特定项目中的 TEI 更新时间更晚，则会下载两套 TEI。
>
>
>*举例说明：*
>* 全球 TEI 限制 = 500
>* 疟疾病例诊断的特定项目 TEI 限制 = 500
>* 全球集包含的最新 TEI 多于特定计划集
>
>设备总共最多可下载 1000 个 TEI，其中 500 个来自全局步骤，500 个来自特定项目步骤。
虽然这初看起来可能出乎意料，但可以有策略地用于保证关键项目的最低 TEI 数量。

***举例****

想象一下，一个名为 "社区关怀 "的项目有 17 个 TEI，都是很久以前更新的。
同一用户还可以访问其他组织单位，其中有许多最近更新的 TEI，这些 TEI 来自其他项目。

确保始终下载社区护理的所有 17 个 TEI：

在 "全局设置 "中设置一个较低的数字（如 20）。

将 17 设置为社区护理的特定计划限额。

将下载初始同步：

20 个最近更新的技术教育指标（非来自社区护理）

17 个社区护理技术教育指标

下载总数 = 37 TEI

#### 重置所有数值{ #capture_app_android_settings_webapp_synchronization_program_reset_all }

点击 *Reset all values* 后，管理员用户将恢复项目部分的默认设置值。请注意，在这种情况下，每个项目都没有特定的设置。 

要保存任何配置，管理员用户需要点击 *Save* 按钮（没有 "ALL "或 "M_androidsettingsapp "权限的用户不能使用该按钮）


### 数据集{ #capture_app_android_settings_webapp_synchronization_data }

本节控制汇总数据同步参数。

#### 全局设置{ #capture_app_android_settings_webapp_synchronization_data_global }

第一部分是全局设置，适用于 Android 用户可以访问的所有数据集。 

![](resources/images/capture-app-dataset-global-settings.png){width=50%}

最大周期数定义了应用项目在数据同步过程中下载的过去周期数。

如果数据集有未来时段（"开放未来时段 "配置），应用项目在限制下载时段数量时不会将其考虑在内。

例如，在 "未来开放期数"=2 和 "最大下载期数"=1 的数据集中，应用项目将下载 3 期。

#### 具体设置{ #capture_app_android_settings_webapp_synchronization_data_specific }

要添加特定设置：

- 单击 * 添加数据集特定设置*，将出现一个包含数据集列表的对话框。 
- 单击数据集，该字段将根据数据集时间类型自动填写默认值。

![](resources/images/capture-app-dataset-specific-dialog.png){width=50%}

![](resources/images/capture-app-dataset-specific-table.png){width=50%}


### 用户同步测试{ #capture_app_android_settings_webapp_synchronization_user_sync_test }

本部分检查用户同步到其设备的数据和元数据的数量。您可以在任何可以访问的用户身上运行此测试。该测试将显示安卓用户可访问的组织单位、数据集、项目规则、项目等的数量（因此安卓应用项目将下载的资源），以及元数据和数据的下载大小（大致估算）。请注意，用户无需拥有 "ALL "或 "M_androidsettingsapp "权限即可运行此测试。

![](resources/images/capture-app-user-sync-test.png){width=50%}

> **注：** 
>
> 红色高亮显示的值是因为该值被认为大于建议的最大值。


## 外观{ #capture_app_android_settings_webapp_appearance }

这些设置可以控制数据输入和列表表单的外观。

- 过滤器：它定义了可以在不同的应用项目菜单中启用的过滤器。
- 完成旋转器：打开/关闭显示数据输入表单进度的完成旋转器。

这些设置涉及可视化组件，因此必须由应用项目使用。
有关过滤器的更多信息，请查阅过滤器 [文档](https://docs.dhis2.org/en/use/android-app/android-specific-features.html#capture_app_generic_filter)。 

### 主屏幕{ #capture_app_android_settings_webapp_appearance_home_screen }

它允许管理员用户启用或禁用在主屏幕上显示与日期、组织单位、同步状态和分配给我相关的筛选器选项。

![](resources/images/capture-app-appearance-home.png){width=75%}


### 计划{ #capture_app_android_settings_webapp_appearance_program }

项目外观允许根据配置需要在项目中隐藏/显示功能。这些更改可以是全局性的，适用于所有项目，也可以是单个项目。

#### 全局设置{ #capture_app_android_settings_webapp_appearance_program_global }

全局设置适用于android用户有权访问的所有程序。

![](resources/images/capture-app-appearance-program-global.png){width=75%}

项目中的完成百分比 (%)： 
启用或禁用显示数据输入表格完成百分比的选项。

TEI 引用（**v2.3.1**）： 
它允许关闭/打开 TEI 引用选项。

折叠表格中的部分（**v2.3.1**）：  
当前行为允许各部分以手风琴样式折叠和展开。该选项可覆盖该行为，使表单（注册和活动）的所有部分保持打开状态，并移除每个部分的 "下一步 "按钮。 


#### 具体设置{ #capture_app_android_settings_webapp_appearance_program_specific }

该部分允许管理员用户自定义表单中的过滤器、完成百分比、强制搜索、推荐、TEI 标题、推荐选项和折叠部分等功能。下文将逐一说明。

要添加特定设置：

- 单击 *添加项目设置*，会出现一个对话框。
- 单击将显示项目列表的下拉菜单。
- 单击项目将显示要配置的不同过滤器。类别组合过滤器取决于类别组合名称。

![](resources/images/capture-app-appearance-program-specific.png){width=75%}

![](resources/images/capture-app-appearance-program-specific-advanced.png){width=75%}

无需搜索的 TEI：
提供在线和离线搜索，并允许用户在没有事先搜索的情况下创建 TEI。

TEI 标头（**v2.3.1**）：
指定一个特定的属性或表达式，在 TEI 卡中显示为标题。 

添加 TEI 标头

- 选择计划指标
- 将显示与项目指示符相关的表达式

![](resources/images/capture-app-appearance-program-specific-tei-header.png)


> **注：**
>
> 如果所选项目缺乏与项目指标相关的表达式，而这些表达式对 Android 有效，则会显示一个通知框。 
> 
> 只有完全由属性和/或函数组成的表达式才被视为对 Android 有效。
> 
>![](resources/images/capture-app-appearance-program-specific-expression-valid.png)
> 
>![](resources/images/capture-app-appearance-program-specific-expression-invalid.png)
> 


如果保存了任何特定设置，则会有一个表格汇总每个项目的特定配置，以及编辑或删除这些设置的选项。 

![](resources/images/capture-app-appearance-program-table.png)

选项 "在项目工具栏中显示完成百分比 (%) "指的是 
![](resources/images/capture-app-appearance-programs-spinner.png){width=50%}

**搜索跟踪实体（v2.2）**

在输入新的 TEI 之前必须先进行搜索，以避免可能出现的重复，DHIS2 安卓应用项目就是这样设计的。新版本允许用户选择保留这一过程，或允许用户在不进行搜索的情况下创建 TEI。
默认情况下，允许用户在不进行搜索的情况下创建 TEI 的功能被禁用。

**捕捉坐标（v2.4.1）**

本节为管理员用户提供了两个可配置的选项，用于管理移动设备上的位置捕捉：

- 禁用手动位置捕捉：
启用此选项后，移动设备上的手动位置输入将被禁用。这样可以确保自动获取位置数据，防止手动错误或更改。

- 最低定位精度：
该字段允许管理员定义捕获位置数据所需的最低精度级别（以米为单位）。数值越低，精度要求越高。建议的最低值为 5 米，以确保可靠和精确的位置捕获。

这些选项可让管理员更好地控制位置数据的采集方式，并确保所记录位置的准确性保持一致。

![](resources/images/capture-app-appearance-programs-coordinates.png)

**快速操作（新 2.4.2）**

快速操作栏是安卓应用项目的一项新功能，允许用户配置和添加代表不同操作的 "芯片"，以便在 TEI 控制面板中轻松访问。
该部分旨在通过为常用功能提供简化和可定制的界面来提高用户的工作效率。

![](resources/images/capture-app-appearance-programs-quick-actions.png)

> **注：**
>
> 选中该复选框将以 "快速访问 "芯片的形式显示操作，而不只是将其列在菜单中。 
> 
> 芯片不能重新排序。出现的顺序是
> - 标记后续行动
> - 转移 TEI
> - 完成注册
> - 取消注册
> - 更多注册
>
> 默认情况下，所有芯片都未选中，快速操作栏在安卓应用项目中不可用。
>
>![](resources/images/capture-app-appearance-program-specific-quick-action.png)
>


### 数据集{ #capture_app_android_settings_webapp_appearance_data }

它允许管理员启用/禁用数据集部分的筛选器

#### 全局设置{ #capture_app_android_settings_webapp_appearance_data_global }

第一部分是适用于android用户有权访问的所有数据集的全局设置。

![](resources/images/capture-app-appearance-dataset-global.png){width=75%}

#### 具体设置{ #capture_app_android_settings_webapp_appearance_data_specific }

要添加特定设置：

- 点击 *添加数据集设置*。将弹出一个对话框，其中有一个数据集列表下拉菜单。
- 点击数据集，将显示启用或禁用筛选器的选项列表。

![](resources/images/capture-app-appearance-dataset-specific.png){width=75%}

![](resources/images/capture-app-appearance-dataset-table.png){width=75%}

**捕捉坐标（新 2.4.2）**

本节为管理员用户提供了两个可配置的选项，用于管理移动设备上的位置捕捉：

- 禁用手动位置捕捉：
  启用此选项后，移动设备上的手动位置输入将被禁用。这样可以确保自动获取位置数据，防止手动错误或更改。

- 最低定位精度：
  该字段允许管理员定义捕获位置数据所需的最低精度级别（以米为单位）。数值越低，精度要求越高。建议的最低值为 5 米，以确保可靠和精确的位置捕获。

这些选项可让管理员更好地控制位置数据的采集方式，并确保所记录位置的准确性保持一致。

![](resources/images/capture-app-appearance-dataset-coordinates.png)



## 分析{ #capture_app_android_settings_webapp_analytics }

分析设置可定义将显示给用户的 TEI、主页、项目和数据集分析项目（图表、表格）。设置应用中定义的任何项目都将覆盖 Android 应用的默认行为，只显示设置应用中定义的项目。 

此外，尽管这些分析是使用安卓设置网络应用项目创建的，但数据聚合仅使用设备中存储的数据离线进行。

### TEI{ #capture_app_android_settings_webapp_analytics_tei }

分析范围是 TEI，因此可视化结果将显示在安卓应用项目的 TEI 面板中。

本节的目的是定义可视化方法，以显示*数据元素*和*计划指标*随时间的演变。在此基础上，它将只考虑属于可重复计划阶段的数据元素，或公式中至少包含一个属于可重复计划阶段的数据元素的计划指标。

创建**TEI 分析**项目：

1. 单击 *添加 TEI 分析*。将弹出一个对话框，其中包含一个小表格。
2. 选择一个项目和一个可重复的项目阶段，然后填写表格。 **短名称**是唯一的可选字段。
3. 如果选择了除 WHO Nutrition 之外的项目可视化，则要选择的下一个字段是周期类型（每月、每周、每日）、元素类型（规划指标、数据元素）以及将基于该元素的元素之前选择的类型。请记住，这些元素与开始时选择的项目和可重复的项目阶段有关。

![](resources/images/capture-app-analytics-item.png)

创建**世卫组织营养分析**项目：

1. 选择计划、计划阶段和世卫组织营养作为可视化类型。
2. 选择世卫组织可视化类型，可以是年龄对应身高 (HFA)、年龄对应体重 (WFA) 或身高对应体重 (WFH)。
3. 选择代表性别的跟踪实体属性。然后，您必须为男性指定 "男性头衔 "选项，为女性指定 "女性头衔 "选项。通常它们都是选项代码。
4. 选择将显示在水平 (x) 轴上的数据元素/项目指示器
5. 选择将显示在垂直（y）轴上的数据元素/项目指示符


![](resources/images/capture-app-analytics-who-item.png)

如果已创建任何 TEI Analytics 项目，表格将显示项目的标题和项目名称，以及删除或编辑该项目的操作按钮。

![](resources/images/capture-app-analytics-table.png)

### 首页{ #capture_app_android_settings_webapp_analytics_home }

主页可视化显示在 Android 应用项目的主屏幕（"分析 "选项卡）上。

所有可用的项目都首先在 DHIS2 的数据可视化器或行列表应用项目中创建，并在安卓设置应用项目中进行配置。

> **注：**
>
> 可视化项目可从 **Data Visualizer** 或 **Line Listing** 应用项目中获取。 
> 
> 选择可视化类型时，显示的项目会自动更改；如果选择 "数据可视化"，则只会显示在数据可视化应用项目中创建的可视化。 
> 
> 选择 "事件可视化"（**v2.4**）时，只有在 "线路列表 "应用项目中创建的元素才会出现在可视化搜索框中。
>

创建**主页**项目：

1. 点击 "添加主页可视化 "按钮。
2. 选择 "可视化类型"（**v2.4**）
3. 点击搜索框，从列表中选择可视化项目或输入可视化项目的名称。
4. 添加替代标题，否则应用项目将显示可视化名称。
5. 默认情况下，应用项目将启用群组可视化设置。
   1. 创建一个新组：将弹出一个自由文本框，输入名称或组名称。
   2. 选择已创建的组可视化：从列表中选择一个选项来添加可视化或
   3. 点击复选框，禁用分组可视化。
6. 点击 "保存 "按钮。

![](resources/images/capture-app-analytics-home-newGroup.png)

![](resources/images/capture-app-analytics-home-createdGroup.png)

> **注：**
>
> 在未选择组的情况下添加的可视化将在一个共同的 "组 "中显示
>
>![](resources/images/capture-app-analytics-default-group.png)
>

编辑**主页**项的标题（**v2.2.1**）：

1. 通过扩展组搜索项目。
2. 点击项目名称旁边的 "编辑 "按钮。
3. 弹出窗口中只有可视化标题可用，其他字段将被禁用。如果需要，可以更改可视化标题。
4. 点击 "保存 "按钮。

![](resources/images/capture-app-analytics-home-editVisualization.png)


要删除**主页**项目：

1. 通过扩展组搜索项目
2. 点击项目名称旁边的 "删除 "按钮
3. 点击 "删除
4. 点击 "保存 "按钮

![](resources/images/capture-app-analytics-home-deleteVisualization.png)

重组组内的**主页**项（**2.4.2**新功能）：

1. 通过扩展组搜索项目
2. 点击项目名称旁边的 "向上 "或 "向下 "箭头按钮
3. 继续点击按钮，直到物品到达想要的位置 
4. 点击 "保存 "按钮

![](resources/images/capture-app-analytics-home-reorganizeVisualization.png)

要删除**主页**组：

1. 搜索要删除的特定组
2. 点击 "删除组
3. 点击 "删除
4. 点击 "保存 "按钮

将删除与该组相关的所有项目

![](resources/images/capture-app-analytics-home-deleteGroup.png)

重置所有数值：

1. 点击 "将所有值重置为默认值"。
2. 点击 "保存 "按钮

### 计划{ #capture_app_android_settings_webapp_analytics_program }

项目可视化显示在跟踪项目的搜索界面（分析选项卡）或安卓应用项目事件项目的列表界面（分析选项卡）中。

所有可用的项目都首先在 DHIS2 的数据可视化器或行列表（**v2.4**）应用项目中创建，并在安卓设置应用项目中进行配置。

> **注：**
>
> 可视化项目可从**数据展示台**或**行列表**应用项目中获取。
>
> 选择可视化类型时，显示的项目会自动更改；如果选择 "数据可视化"，则只会显示在数据可视化应用项目中创建的可视化。
>
> 选择 "事件可视化"（**v2.4**）时，只有在 "线路列表 "应用项目中创建的元素才会出现在可视化搜索框中。
>
>![](resources/images/capture-app-analytics-program-visualization-type.png)
> 

创建**项目**项目：

1. 点击 "添加项目可视化 "按钮。
2. 选择可视化类型（**v2.4**）。
3. 选择计划。
4. 点击搜索框，从列表中选择可视化项目或输入可视化项目的名称。
5. 添加替代标题，否则应用项目将显示可视化名称
6. 默认情况下，应用项目将启用群组可视化设置。
   1. 创建一个新组：将弹出一个自由文本框，输入名称或组名称。
   2. 选择已创建的组可视化：从列表中选择一个选项来添加可视化或
   3. 点击复选框，禁用分组可视化。
7. 点击 "保存 "按钮。

![](resources/images/capture-app-analytics-program-add.png)

编辑**项目**项的标题（**v2.2.1**）：

1. 通过扩展组搜索项目。
2. 点击项目名称旁边的 "编辑 "按钮。
3. 弹出窗口中只有可视化标题可用，其他字段将被禁用。如果需要，可以更改可视化标题。
4. 点击 "保存 "按钮。

![](resources/images/capture-app-analytics-program-editVisualization.png)

要删除**项目**项目：

1. 通过扩展项目和组来搜索项目
2. 点击项目名称旁边的 "删除 "按钮
3. 点击 "删除
4. 点击 "保存 "按钮

![](resources/images/capture-app-analytics-program-deleteVisualization.png)

重组组内的**项目**项（**新 2.4.2**）：

1. 通过扩展项目和组来搜索项目
2. 点击项目名称旁边的 "向上 "或 "向下 "箭头按钮
3. 继续点击按钮，直到物品到达想要的位置
4. 点击 "保存 "按钮

![](resources/images/capture-app-analytics-program-reorganizeVisualization.png)

要删除**项目**组：

1. 在相应项目中搜索要删除的特定组
2. 点击 "删除组
3. 点击 "删除
4. 点击 "保存 "按钮

将删除与该组相关的所有项目

![](resources/images/capture-app-analytics-program-deleteGroup.png)

重置所有数值：

1. 点击 "将所有值重置为默认值"。
2. 点击 "保存 "按钮

### 数据集{ #capture_app_android_settings_webapp_analytics_data }

数据集可视化显示在安卓应用项目数据集的列表屏幕（分析选项卡）中。

所有可用的项目都首先在 DHIS2 的数据可视化器应用项目中创建，并在安卓设置应用项目中进行配置。

创建**数据集**项目：

1. 点击 "添加数据集可视化
2. 选择数据集
3. 点击搜索框，从列表中选择可视化项目或输入可视化项目的名称。
4. 添加替代标题，否则应用项目将显示可视化名称
5. 默认情况下，应用项目将启用群组可视化设置。
   1. 创建一个新组：将弹出一个自由文本框，输入名称或组名称。
   2. 选择已创建的组可视化：从列表中选择一个选项来添加可视化或
   3. 点击复选框，禁用分组可视化。
6. 点击 "保存 "按钮。

![](resources/images/capture-app-analytics-dataset-add.png)

编辑**数据集**项的标题（**v2.2.1**）：

1. 通过扩展组搜索项目。
2. 点击项目名称旁边的 "编辑 "按钮。
3. 弹出窗口中只有可视化标题可用，其他字段将被禁用。如果需要，可以更改可视化标题。
4. 点击 "保存 "按钮。

![](resources/images/capture-app-analytics-dataset-editVisualization.png)

删除**数据集**项目：

1. 通过展开数据集和分组搜索项目
2. 点击项目名称旁边的 "删除 "按钮
3. 点击 "删除
4. 点击 "保存 "按钮

![](resources/images/capture-app-analytics-dataset-deleteVisualization.png)

重组组内的**数据集**项（**新 2.4.2**）：

1. 通过展开数据集和组搜索项目
2. 点击项目名称旁边的 "向上 "或 "向下 "箭头按钮
3. 继续点击按钮，直到物品到达想要的位置
4. 点击 "保存 "按钮

![](resources/images/capture-app-analytics-dataset-reorganizeVisualization.png)

删除**数据集**组：

1. 在相应的数据集中搜索要删除的特定组
2. 点击 "删除组
3. 点击 "删除
4. 点击 "保存 "按钮

将删除与该组相关的所有项目

![](resources/images/capture-app-analytics-dataset-deleteGroup.png)

重置所有数值：

1. 点击 "将所有值重置为默认值"。
2. 点击 "保存 "按钮

### 可视化用户测试{ #capture_app_android_settings_webapp_analytics_visualization_user_test }

可视化用户测试是 "主页"、"项目 "和 "数据集分析 "设置中的一项功能，可帮助管理员用户识别是否有特定用户可以查看可视化。

安卓设置项目会检查这三种权限中的至少一种：

1. 可视化是公开的
2. 用户可单独访问可视化系统
3. 用户所在的组有可视化访问权限

运行测试：

1. 从列表中选择用户
2. 点击 "运行测试

![](resources/images/capture-app-analytics-usertest-access.png)

![](resources/images/capture-app-analytics-usertest-access-visualization.png)

![](resources/images/capture-app-analytics-usertest-noaccess.png)

![](resources/images/capture-app-analytics-usertest-noaccess-visualization.png)

### 分析的局限性{ #capture_app_android_settings_analytics_limitations }

由于显示的汇总和计算是在设备中进行的，因此与网络相比，分析的实施受到了限制。 

总之，数据可视化兼容和支持的对象和功能有

- 完善的分析对象（序列、类别、筛选器）
- 使用相对时段（不支持固定时段）
- 用户有查看权限
- 透视表的限制
  - 标题行数：1 或 2 (*v2.3*)
  - 标题栏数量：2
- 图表的限制
  - 系列数量：无限制（但请记住，您是在小屏幕上进行渲染）
  - 类别数（不适用于饼图）无限制

关于网络展示台中的许多配置选项以及与指标和项目指标相关的支持功能和计算，还有许多限制适用于 Android Analytics。[本表](https://docs.google.com/spreadsheets/d/1127cz7M0K4fux5CU0V54V2Z77NZWCr0BTrZ6jcCec4Q) 总结了所有支持的功能。

同样，事件可视化在以下情况下被视为有效：

- 使用线条列表应用项目创建可视化
- 可视化类型为线条列表
- 使用相对时期
- 表格的限制
  - 列数15


> 注意（v2.2.1）**
>
> 搜索要添加到分析可视化中的可视化时，将列出所有可视化，但不符合上述限制的可视化将被禁用。 
>
>![](resources/images/capture-app-analytics-visualization-search.png)
>

## 自定义意图（新增 3.3.0）{ #capture_app_android_settings_webapp_custom_intents }

通过配置自定义意图，管理员可以让 Android 应用项目启动外部应用项目，向其发送数据，并接收返回的数据以自动填充字段。

### 添加自定义意图{ #capture_app_android_settings_webapp_custom_intents_configuration_adding }

要添加新的自定义意图，请

1. 打开安卓设置网络应用项目
2. 导航至自定义意图部分
3. 点击 **添加自定义意图**
4. 如下所示填写配置参数

![](resources/images/capture-app-image365.png)
![](resources/images/capture-app-image361.png)

### 配置参数{ #capture_app_custom_intents_configuration_parameters }

#### 基本信息{ #capture_app_android_settings_webapp_custom_intents_configuration_basic }

意图名称
:   自定义意图的唯一描述性名称。该名称有助于在配置界面中识别意图。

意图说明
:   详细描述自定义意图的作用以及与哪个第三方应用项目集成。这有助于其他管理员了解该意图的目的。

#### 元素绑定{ #capture_app_android_settings_webapp_custom_intents_configuration_binding }

元素类型
:   指定自定义意图的附加位置。选项包括

    - ** 跟踪实体属性**：将意图链接到特定的跟踪实体属性
    - 数据元素**：将意图链接到特定数据元素

属性/数据元素
:   选择将触发自定义意图的特定跟踪实体属性或数据元素。为字段配置自定义意图后，用户必须使用第三方应用项目为该字段输入或搜索数据。手动数据输入将不可用。

> **注**
>
> 目前，自定义意图只支持具有 **TEXT** 或 **LONG_TEXT** 值类型的数据元素或跟踪实体属性。

屏幕/动作
:   定义自定义意图将在 Android 应用项目中的哪个位置触发。可用选项

    - 搜索**：该意图将在搜索操作期间可用，允许用户使用外部应用程序的数据搜索跟踪的实体实例
    - **数据录入**：该意图将在数据录入过程中使用，允许用户使用外部应用程序的数据填充字段

#### 第三方应用项目{ #capture_app_android_settings_webapp_custom_intents_configuration_app }

软件包名称
:   要启动的第三方 Android 应用项目的完整软件包名称和操作。其中应包括软件包标识符和具体操作。

    格式：com.apppackageName.id.CAPTURE

    示例： `com.google.zxing.client.android.SCAN`用于条形码扫描器应用程序

> 重要**
>
> 第三方应用程序必须安装在安卓设备上，自定义意图才能正常工作。如果未安装应用程序，用户在尝试触发意图时将收到错误信息。

### 请求配置{ #capture_app_android_settings_webapp_custom_intents_configuration_request }

请求 "部分定义了启动意图时将发送给第三方应用项目的参数。这样，您就可以根据自己的要求定制外部应用项目的行为。

#### 请求参数{ #capture_app_android_settings_webapp_custom_intents_configuration_request_params }

请求参数
:   键值对列表，将作为额外内容发送给第三方应用项目。每个参数包括

    - 键**：第三方应用程序期望的参数名称
    - 值**：要发送的参数值

值 "参数是一个表达式，可根据需要分配动态值。

它支持项目规则中使用的 d2 函数，并支持一些变量。支持的变量有

| 变量      | 描述                                     |
|---------------|-------------------------------------------------|
| 代码  | TE/Event 组织单位代码             |
| orgunit_id    | TE/Event 组织单位的 UID              |
| orgunit_path  | TE/Event orgunit 的路径             |
| user_id       | 登录用户的 UID                          |
| 用户名 | 登录用户的用户名                     |

这些变量的标识符是前缀 `VAR`。例如，你可能有这样一个值

```
VAR(orgunit_id)
```
或
```
d2:condition('OU_', VAR{orgunit_id}) // 结果类似于'OU_DiszpKrYNg8'。
```

> **注**
>
> 请求参数是类型安全的。请遵循以下格式规则：
> - **字符串**：必须用单引号包围（例如，`'QRCODE'`, `'portrait'`)
> - **整数**：无需引号（例如，`100`, `640）
> - **浮点数**：无需引号（例如，`3.14`, `2.5）
> - **布尔值**：无需引号（例如，`true`, `false）
> - **表达式**：无需引号（例如，`VAR{orgunit_id}`)

**请求参数示例：**

| 键          | 值       | 描述                   |
|--------------|-------------|-------------------------------|
| 项目编号   | 样本 ID | 第三方应用项目需要 ID |
| 方向  | 肖像  | 设置摄像头方向        |
| 超时      | 30000       | 以毫秒为单位设置超时   |
| ENABLE_FLASH | 真正        | 启用相机闪光灯           |

![](resources/images/capture-app-image360.png)

### 响应配置{ #capture_app_android_settings_webapp_custom_intents_configuration_response }

响应部分定义了如何提取和处理从第三方应用项目返回的数据。这决定了在链接的跟踪实体属性或数据元素中将填充哪个值。

#### 响应参数{ #capture_app_android_settings_webapp_custom_intents_configuration_response_params }

附加名称
:   第三方应用项目返回的意图中额外字段的确切名称。这是外部应用项目向 DHIS2 发送数据时使用的密钥。

    例如：`SCAN_RESULT`、`LATITUDE`、`BARCODE_VALUE

额外类型
:   返回值的数据类型。可用选项：

    - 字符串**：纯文本值
    - 整数**：整数值
    - 浮点数**：小数数值
    - 布尔值真/假值
    - **JSON 对象**：单个 JSON 对象
    - JSON 对象列表**：一个 JSON 对象数组

JSON 对象中的属性
:   *仅适用于 Extra 类型为 "JSON 对象 "或 "JSON 对象列表 "时。

    指定要从 JSON 对象中提取的属性名称。该名称应是 JSON 对象的直接属性。

    示例：`value`, `latitude`, `name

> **注**
>
> 只支持直接属性。目前不支持嵌套属性（例如，`location.coordinates.latitude`）。
> - 目前不支持数组索引（例如，`results[0].name`）。
> - 使用**JSON 对象列表**作为额外类型时：
> - 对于 **搜索**：将从列表中的所有对象中提取指定属性，允许用户搜索与列表中任意值匹配的被跟踪实体实例。
> - 对于 **DATA_ENTRY**：将从所有对象中提取值，并串联成一个以逗号分隔的字符串（例如，`value1,value2,value3`）。

![](resources/images/capture-app-image362.png)

### 使用案例和示例{ #capture_app_android_settings_webapp_custom_intents_examples }

#### 示例 1：条形码扫描仪集成{ #capture_app_android_settings_webapp_custom_intents_examples_barcode }

此示例展示了如何集成条形码扫描仪应用项目以填充患者 ID 字段。

**配置：**
- **意图名称**：病人身份条码扫描仪
- **目的描述**：使用 ZXing 扫描仪扫描病人 ID 条形码
- **元素类型**：跟踪实体属性
- **属性**：患者 ID
- **屏幕/操作**：DATA_ENTRY
- **软件包名称**：com.google.zxing.client.android.SCAN

**请求参数：**
| 键值
|-----|-------|
| 扫描模式
| PROMPT_MESSAGE | 'Scan patient ID barcode' | '扫描病人 ID 条形码' | '扫描病人 ID 条形码' | '扫描病人 ID 条形码

**答复：**
- **额外名称**：扫描结果
- **额外类型**：字符串
- JSON 对象中的**属性**：*字符串类型留空）*

#### 示例 2：GPS 坐标集成{ #capture_app_android_settings_webapp_custom_intents_examples_gps }

此示例演示了从一个返回 JSON 数据的地图应用项目中获取 GPS 坐标精度。

**配置：**
- **目标名称**：GPS 位置捕获
- **意图描述**：从地图应用项目捕捉 GPS 坐标
- **要素类型**：数据元素
- **数据元素**：住户地点
- **屏幕/操作**：DATA_ENTRY
- **软件包名称**：com.example.gpsapp.CAPTURE_LOCATION

**请求参数：**
| 键值
|-----|-------|
| 准确度 | 'high' | | 时间
| 超时时间：60000

**答复：**
- **额外名称**：位置数据
- **额外类型**：JSON 对象
- JSON 对象中的**属性**：精度

**JSON 响应示例：**
``json
{
  "coordinates"：{
    "latitude"：-1.2921,
    经度36.8219
  },
  "精确度"：5.0,
  "timestamp"："2025-11-03T10:30:00Z"
}
```

#### 示例 3：自定义数据收集工具{ #capture_app_android_settings_webapp_custom_intents_examples_custom }

本示例展示了与自定义医疗设备应用项目的集成，该应用项目在 JSON 数组中返回多个测量值。

**配置：**
- **意图名称**：血压计
- **意图描述**：从外部监测设备采集血压
- **要素类型**：数据元素
- **数据元素**：收缩压
- **屏幕/操作**：DATA_ENTRY
- **软件包名称**：com.medical.bpmonitor.MEASURE

**请求参数：**
| 键值
|-----|-------|
| 测量类型 | "血压" | 单位
| 单位 | 'mmHg' | '毫米汞柱' | '毫米汞柱' | 'mmHg

**答复：**
- **额外名称**：测量
- **额外类型**：JSON 对象列表
- JSON 对象中的**属性**：收缩

**JSON 响应示例：**
``json
[
  {
    "收缩压"： 120
    "舒张压"： 80
    "脉搏"：72,
    "时间戳"："2025-11-03T10:30:00Z"
  }
]
```
### 局限性和考虑因素{ #capture_app_android_settings_webapp_custom_intents_limitations }

#### 第三方应用项目要求{ #capture_app_custom_intents_limitations_app }

- 第三方应用项目必须安装在所有将使用自定义意图的 Android 设备上
- 第三方应用项目必须支持基于 Android Intent 的通信
- 必须正确配置软件包名称和操作，并与第三方应用项目的实现相匹配

#### 数据类型兼容性{ #capture_app_android_settings_webapp_custom_intents_limitations_datatype }

- 响应中配置的额外类型必须与第三方应用项目返回的实际数据类型一致
- 跟踪的实体属性或数据元素值类型应与返回的数据兼容
- 类型不匹配可能导致错误或意外行为

#### 设备兼容性{ #capture_app_android_settings_webapp_custom_intents_limitations_device }

- 自定义 Intents 依赖于 Android 的 Intent 系统，仅适用于 Android 设备
- 某些第三方应用项目可能有特定的 Android 版本要求
- 应在所有目标设备型号上进行测试

#### 安全考虑因素{ #capture_app_android_settings_webapp_custom_intents_limitations_security }

> **警告**
>
> 配置与第三方应用项目集成的自定义意图时，请考虑以下安全影响：
> - 发送到第三方应用项目的数据可能会根据第三方应用项目的隐私政策进行存储或传输
> - 确保第三方应用项目来自可信来源
> - 审查第三方应用项目请求的权限
> 敏感的健康数据只能与经过认证且合规的应用项目共享
> 考虑实施移动设备管理 (MDM)，以控制可安装的应用项目

#### 网络和离线行为{ #capture_app_android_settings_webapp_custom_intents_limitations_offline }

- 只要安装了第三方应用项目且不需要网络连接，自定义意图就能在离线模式下工作
- 某些第三方应用项目可能需要访问互联网才能正常运行
- 确保解决方案在部署的预期连接场景中正常运行

### 故障排除{ #capture_app_android_settings_webapp_custom_intents_troubleshooting }

#### 常见问题{ #capture_app_android_settings_webapp_custom_intents_troubleshooting_common }

**问题：自定义意图按钮不显示**
- 验证自定义意图是否已在 Android 设置网络应用项目中正确配置
- 检查设备是否同步了最新配置
- 确保查看正确的跟踪实体属性或数据元素

**问题："未找到应用项目 "错误**
- 设备上未安装第三方应用项目
- 软件包名称配置不正确
- 第三方应用项目不支持指定操作

**问题：没有返回数据**
- 验证 "额外名称 "与第三方应用项目使用的密钥是否匹配
- 检查额外类型是否正确
- 查看第三方应用项目的文档，了解预期的响应格式

**问题：输入了错误的值**
- 验证 JSON 对象路径中的属性是否正确
- 检查 JSON 结构是否符合预期格式
- 确保额外类型与实际返回的数据类型一致

**问题：启动意图时应用项目崩溃**
- 检查请求参数是否有语法错误（如字符串缺少引号）
- 验证第三方应用项目是否支持所配置的参数
- 查看 Android 日志，了解详细的错误信息

#### 测试自定义意图{ #capture_app_android_settings_webapp_custom_intents_troubleshooting_testing }

将自定义意图部署到生产环境之前

1. 在不同型号的设备上对少数用户进行测试
2. 验证第三方应用项目是否可用并获得适当许可
3. 同时测试在线和离线场景
4. 验证数据验证规则是否适用于返回值
5. 测试未安装第三方应用项目时的错误处理
6. 记录配置和用户说明

> **注**
>
> 在部署到生产项目之前，考虑创建一个测试项目或数据集，专门用于测试自定义意图配置。

### 最佳做法{ #capture_app_custom_intents_best_practices }

1. **明确命名**：使用描述性的意图名称，明确指出使用目的和第三方应用项目

2. **文档**：维护所有已配置自定义意图的文档，包括所需的第三方应用项目及其版本

3. ** 用户培训**：向用户提供清晰的说明，告诉他们如何使用自定义意图以及需要注意的事项。确保用户明白，对于配置了自定义意图的字段，将无法使用手动数据录入。

4. **应用分发**：使用移动设备管理（MDM）确保在所有设备上正确安装和更新第三方应用项目

5. **测试**：部署前在非生产环境中彻底测试自定义意图

6. **验证**：对使用自定义意图的字段实施适当的验证规则，以确保数据质量

7. **可靠性**：确保第三方应用项目稳定可靠，因为它是填充配置字段的唯一途径。如果第三方应用项目不可用，请制定应急计划。

8. **版本控制**：跟踪第三方应用项目版本，并在应用项目更新时测试兼容性

9. **隐私合规性**：确保自定义意图配置符合数据隐私法规（GDPR、HIPAA 等）

## 安装{ #capture_app_android_settings_webapp_installation }

用户可以登录DHIS2并转到**应用程序管理**来轻松安装Android设置网络应用程序。

- 点击 ** 应用项目中心**
- 进入 *安卓设置应用项目*。
- 点击 *安装 V2.X.X*

![](resources/images/capture-app-app-hub-install.png)
![](resources/images/capture-app-app-hub-install-webapp.png)


## 登录和首次设置{ #capture_app_android_settings_webapp_login }

用户首次安装并启动安卓设置 Web 应用项目后，Web 应用项目将要求设置并保存配置的默认值。这将对连接到实例的所有安卓设备应用默认设置。 

![](resources/images/capture-app-first-time-setup.png){width=50%}

> **警告**
>
> 请注意，以前的版本已经过时，因此您将从新的默认配置开始。

![](resources/images/capture-app-first-setup-with-deprecation-message.png){width=50%}

> 警告 (v2.3)** 
>
> 当使用的 DHIS2 版本等于或高于 2.40 时，只有拥有 "M_androidsettingsapp "或 "ALL "权限的用户才能*保存或更新*配置，但任何用户在配置创建后都有*查看*权限。
> 


![](resources/images/capture-app-first-setup-no-authorities.png){width=50%}


## 输入并保存配置参数{ #capture_app_android_settings_webapp_enter_and_save }

### 数据存储{ #capture_app_android_settings_webapp_datastore }

在内部，所有设置都以 JSON 格式存储在 [Datastore](https://docs.dhis2.org/en/develop/using-the-api/dhis-core-version-240/data-store.html) 中。

数据存储结构：

| 项目      | 描述                                 | 数据类型 |
|-----------|---------------------------------------------|-----------|
| 命名空间 | 条目组织的命名空间       | 串    |
| 键       | 识别价值的钥匙            | 串    |
| 值     | 包含条目信息的值 | JSON格式      |

### 保存配置参数{ #capture_app_android_settings_webapp_save_config }

在所有设置部分的表单页脚，管理员用户可以找到一个*保存*按钮。

![](resources/images/capture-app-save_button.png)

只有当管理员用户单击此按钮时，对当前部分所做的所有更改才会保存在数据存储中。这些更改将在同步配置时应用到 Android 捕捉应用项目。

未保存的更改
:   如果管理员用户想转到另一个部分，但仍有一些未保存的更改，则在离开当前部分前会显示提示。如果用户接受离开页面，则未保存的更改将丢失。

![](resources/images/capture-app-unsaved-changes.png)

#### 将所有值重置为默认值{ #capture_app_android_settings_webapp_reset_all }

管理员用户可以随时恢复设置。所有设置部分的表单页脚都有一个*将所有值还原为默认值*按钮。 

![](resources/images/capture-app-reset-default.png)

默认值只会在数据存储上恢复，并在点击*保存*后应用。 


## 卸载应用项目{ #capture_app_android_settings_webapp_uninstall_app }

如果需要卸载 Android 设置应用项目，建议同时删除保留数据存储命名空间中的所有数据。

#### 删除数据{ #capture_app_android_settings_webapp_delete_data }

- 转到*常规*部分
- 点击 *禁用所有设置*。
- 将弹出一个模式窗口，点击*禁用*。

![](resources/images/capture-app-unistall-delete-namespace.png)

![](resources/images/capture-app-unistall-delete-namespace-modal.png)

#### 卸载应用项目{ #capture_app_android_settings_webapp_uninstall_app }

- 转到 **应用项目管理**
- 点击**自定义应用项目**
- 进入 *安卓设置应用项目*。
- 点击 * 卸载 V2.X.X*

![](resources/images/capture-app-unistall-custom-app.png)

![](resources/images/capture-app-unistall-aswa.png)

> **警告**
>
> 这是一项关键操作，请注意，执行此操作后，之前的所有配置都将被永久删除。
>


## 应用项目兼容性{ #capture_app_android_settings_webapp_app_compatibility }

系统会显示一个标签，标明使用特定特性或功能所需的最低 Android 应用项目版本。这可确保用户了解兼容性要求，并在必要时更新其应用项目，以满足最低版本的要求。

例如，如果某项功能需要 Android 应用项目版本为 2.9 或更高，标签就会清楚地向用户显示所需的版本。

![](resources/images/capture-app-aswa-compatibility.png)

[本表](https://docs.google.com/spreadsheets/d/1HZv4iYD9_UVbMAxEgIHYXD_peK_ppzpJLo85MojNmMQ/edit?usp=sharing) 总结了所有支持的功能及其与 Android Capture 应用项目的兼容性。

