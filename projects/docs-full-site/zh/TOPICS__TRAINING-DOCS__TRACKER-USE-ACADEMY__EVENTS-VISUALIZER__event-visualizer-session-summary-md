---
edit_url: "https://github.com/dhis2/training-docs/blob/main/content/tracker_use/summary_event_visualizer.md"
revision_date: '2024-07-08'
tags:
- 培训文件
---

# 活动报告 - 会议摘要{ #event-reports-session-summary } 

事件可视化应用程序允许您根据跟踪器数据创建图表。它尤其适用于标准化响应（即选项集信息）的可视化。

创建使用事件作为输出类型的可视化时，事件和跟踪器数据的处理方式相同，因为在事件可视化器中只能选择一个程序阶段进行处理。注册 "输出类型在此应用程序中不起作用。

事件可视化器中有一种 "跟踪实体实例 "输出类型。它只适用于跟踪器数据（而非事件数据），因为它计算的是单个程序中符合给定条件的唯一跟踪实体实例的数量。

## 创建事件可视化{ #creating-an-event-visualization } 

1. 选择图表类型
2. 选择您的计划和计划阶段
3. 选择您的数据
4. 为所选数据定义筛选器
5. 选择您的时间段
6. 选择您的组织单位
7. 修改布局
8. 更新您的可视化

By default, an event visualization will use event as its output type. This means these visualizations are summarizing **all events** within a ***single*** program stage, including any repeated events within the stage you have selected.

## 跟踪实体输出类型{ #tracked-entity-output-type } 

我们可以修改图表以使用输出类型 "跟踪实体实例"。为此，我们要选择图表选项菜单并修改输出类型。这将计算符合标准的 TEI 数量。因此，重复事件数据将被排除在此输出之外。 

不过，与注册类型输出不同，在事件可视化中使用 TEI 输出类型时，我们无法查看多个阶段的数据。

请注意，虽然该输出类型列表中存在 enrollment，但它不起作用，应予以忽略。

## 注册输出与跟踪实体实例输出对比{ #enrollment-outputs-vs-tracked-entity-instance-outputs } 

为什么事件报告和事件可视化器在输出类型上使用不同的语言？事件报告中的 "注册 "输出类型允许您显示注册的唯一信息，以及获取整个注册的数据（这意味着您可以在行列表中一起查看多个计划阶段的数据；并通过透视表获取唯一计数）。事件可视化器中的 "跟踪实体实例 "输出类型限制较多。它不允许你从多个计划阶段提取数据；它只是让你计算在一个计划阶段内符合你通过筛选器设置的标准的唯一跟踪实体（通常是人）的数量。这可能很有用，但重要的是要区分开来。

## 保存事件可视化{ #saving-an-event-visualization } 

1. 选择收藏夹菜单
2. 选择 "另存为"。
3. 为报告命名并保存

## 下载事件可视化{ #downloading-an-event-visualization } 

1. 选择下载菜单
2. 选择下载报告的格式
3. 从下载文件夹中提取下载的报告，打开报告

