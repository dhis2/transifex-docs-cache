---
edit_url: "https://github.com/dhis2/dhis2-docs/blob/master/src/commonmark/en/content/tutorials/enable-spdy.md"
revision_date: '2020-12-14'
title: 如何使用 nginx 为 DHIS 2 启用 SPDY
---

# 如何使用 nginx 为 DHIS 2 启用 SPDY { #how-to-enable-spdy-with-nginx-for-dhis-2 }

[SPDY](http://en.wikipedia.org/wiki/SPDY) 操纵 HTTP 流量，旨在减少网页加载时间并提高网络安全性。 SPDY 通过压缩、复用和优先级来减少延迟。所有主流浏览器的更高版本都支持 SPDY。要学习本教程，您需要具备以下条件：

*  运行 DHIS 的 Linux Web 服务器
*  SSL 证书
*  作为反向代理运行的 nginx http 服务器

首先，验证您是否有安装了 SPDY 模块的更高版本的 nginx：

```
$ nginx -V
```

Make sure you see `--with-http_spdy_module` somewhere in the output. If not then you need to add the nginx stable repository and [install the latest stable version](http://wiki.nginx.org/Install). SPDY support was introduced in version 1.5.10\. To enable SPDY first open your nginx.conf configuration file, move down to your SSL server block and change this line:

```
listen 443 ssl；
```

对此：

```
listen 443 ssl spdy；
```

并使用以下命令重新加载 nginx：

```
$ sudo /etc/init.d/nginx reload
```

就是这样。新访问者应该获得 SPDY 提供的内容。要验证您的设置，您可以使用 [spdycheck.org](http://spdycheck.org/). If you are using the Chrome browser you can type `chrome://net-internals/#spdy` in the address bar and look for your site after you have visited it. Note that nginx SPDY is still [experimental](http://nginx.org/en/docs/http/ngx_http_spdy_module.html) 上的在线检查。
