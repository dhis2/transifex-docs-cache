---
edit_url: "https://github.com/dhis2-metadata/NCD_CR/blob/master/docs/ncd_cr-installation.md"
revision_date: '2026-03-18'
tags:
- 实施
---

# 癌症登记处 (CR) 安装指南{ #ncd-cr-installation }

本文档包括癌症登记软件包的安装指南。

系统默认语言：系统默认语言：英语

## order is always case-sensitive

元数据参考和元数据 json 文件提供了软件包版本和内容的技术细节。

元数据包由以下模块组成：

- 癌症登记处

## 安装 { #installation } 

模块的安装包括以下几个步骤：

1. [用 DHIS2 元数据准备元数据文件](#preparing-the-metadata-file)
2. [将元数据文件导入 DHIS2](#importing-metadata)
3. [配置导入的元数据](#configuration)
4. [导入后调整程序](#调整程序)

建议在开始 DHIS2 的安装和配置过程之前，先通读安装指南的每个章节。
DHIS2 的安装和配置过程。根据导入类型确定适用的章节：

1. 导入空白的 DHIS2 实例
2. 导入具有现有元数据的 DHIS2 实例。

本文档中概述的步骤应在测试/暂存 DHIS2 实例中进行测试，然后才应用于生产环境。
生产环境。

## 要求 { #requirements } 

安装模块需要 DHIS2 的管理员用户账户。

应格外小心，以确保服务器本身和 DHIS2 应用程序的安全，并应定义对所收集数据的访问权限。
应定义对所收集数据的访问权限。有关 DHIS2 系统安全的详细信息不在本文件讨论范围之内，请参阅[DHIS2 文档]()。
DHIS2 文档](https://docs.dhis2.org/)。

## 元数据文件{ #metadata-files } 

虽然并非总是必要，但在将元数据文件导入 DHIS2 之前对其进行某些修改往往是有好处的。
将其导入 DHIS2。

## 准备元数据文件 { #preparing-the-metadata-file } 

在导入元数据文件之前，需要对其进行一些更改。工作范围可能因
不同的软件包。

> **注**
>
> 请注意，必须使用纯文本编辑器，而不是 Microsoft
> Word。

## 导入元数据 { #importing-metadata } 

使用 [导入/导出](#import_export) DHIS2 应用程序导入元数据包。建议使用 "模拟运行 "功能
以发现问题，然后再尝试实际导入元数据。如果 "模拟运行 "报告有任何问题或
冲突，请参阅下文的 [导入冲突]（#处理-导入-冲突）部分。如果 "干运行"/"验证 "导入
无误，请尝试导入元数据。如果导入成功且无任何错误，则可以继续
配置](#configuration)模块。在某些情况下，"干运行 "时不会显示导入冲突或问题，但在实际导入时会显示。
在某些情况下，"模拟运行 "时不会显示导入冲突或问题，但在尝试实际导入时会显示。在这种情况下，导入摘要将列出需要解决的错误。
需要解决的错误。

### 处理导入冲突 { #handling-import-conflicts } 

> **注**
>
如果将软件包导入新的 DHIS2 实例，不会出现导入冲突，因为目标数据库中没有 > 元数据。
> 目标数据库中没有元数据。导入元数据后，进入"[配置](#configuration) "部分。

可能会发生多种冲突，但最常见的是配置包中的元数据对象的名称、简称和/或代码
配置包中的元数据对象的名称、简称和/或代码已存在于目标数据库中。解决这些问题的
这些问题有几种不同的解决方案，各有利弊。哪一种更合适
例如，取决于发生冲突的对象类型。

#### 备选方案1 { #alternative-1 } 

重新命名 DHIS2 数据库中存在冲突的现有对象。这种方法的优点是
无需修改 .json 文件，因为更改是通过 DHIS2 的用户界面完成的。这
可能更少出错。这也意味着配置包将保持原样，这在以下情况下是一个优势
例如，在发布软件包更新时。原始软件包对象也经常在培训材料和文档中被引用。
材料和文档。

#### 备选方案2 { #alternative-2 } 

重命名 .json 文件中存在冲突的对象。这种方法的优点是
DHIS2 元数据保持不变。当有培训材料或文档（如与相关对象链接的数据字典的 SOP）时，这可能是一个因素。
当有培训材料或文件（如数据字典的 SOP）链接到相关对象时，这可能是一个因素，而且不会因修改用 户熟悉的元数据而有任何混淆的风险。
元数据。

请注意，对于备选方案 1 和 2，修改可以很简单，只需在名称中添加一个小的前缀/后缀，以尽量减少混淆的风险。
将混淆的风险降至最低。

#### 备选3 { #alternative-3 } 

第三种更复杂的方法是修改 .json 文件，重新使用现有的元数据。例如
例如，在某个概念（如 "性别"）的选项集已经存在的情况下，可以将该选项集从 .json
文件中删除该选项集，并用数据库中已有的相应选项集替换对其 UID 的所有引用。这样做的最大好处
这样做的最大好处（不限于直接导入冲突的情况）是避免在数据库中创建重复的元数据。
元数据。在进行此类修改时，有一些关键的注意事项：

- 它需要有关DHIS2详细元数据结构的专业知识
- 这种方法并不适用于所有类型的对象。特别是，某些类型的对象有依赖关系，这些依赖关系
  用这种方法解决这些问题比较复杂，例如与分类有关的问题。
- 将来对配置包的更新将很复杂。

## 组态 { #configuration } 

成功导入所有元数据后，在模块开始运行前还需要采取以下几个步骤
功能。

### 分享中 { #sharing } 

首先，您必须使用 DHIS2 的*共享*功能来配置哪些用户（用户组）应查看与程序相关的元数据和数据，以及谁可以在程序中注册/输入数据。
元数据和与程序相关的数据，以及谁可以在程序中注册/输入数据。默认情况下
共享已配置如下：

- IndicatorGroup

有关共享的更多信息，请参阅 [DHIS2 文档](#sharing)。

软件包包括三个核心用户组：

- CR - 访问（查看元数据/查看数据）
- CR - 管理（查看和编辑元数据/无法访问数据）
- CR - 数据采集 - （查看元数据/采集和查看数据）

用户会根据其在系统中的角色被分配到相应的用户组。软件包中其他对象的共享
软件包中其他对象的共享可根据设置进行调整。更多信息请参阅 [DHIS2 共享文档](#sharing)。
更多信息。

### 用户角色 { #user-roles } 

用户需要有用户角色才能使用 DHIS2 的各种应用程序。建议使用以下最低角色
建议如下

1. 跟踪器数据分析：可查看事件分析并访问仪表板、事件报告、事件可视化器和数据。
   可视化工具、数据透视表、报告和地图。
2. 跟踪器数据捕获：可添加数据值、更新跟踪实体、跨组织单位搜索跟踪实体，以及
   访问跟踪器捕获

有关配置用户角色的更多信息，请参阅 [DHIS2 文档](https://docs.dhis2.org/)。

### 组织单位分配{ #organisation-unit-assignment } 

程序必须分配给现有层次结构中的组织单位，才能通过捕获
应用程序。

### 重复的元数据 { #duplicated-metadata } 

> **注**
>
> 本节仅适用于导入已有元数据的 DHIS2 数据库。如果
> 您正在使用一个新的 DHIS2 实例，请跳过本节，转到
> 转到 [调整跟踪程序](#adapting-the-tracker-program)。
> 如果您正在使用依赖于当前元数据的任何第三方应用程序，请考虑到此
> 更新可能会破坏它们

即使在成功导入元数据且没有任何导入冲突的情况下，元数据中也可能存在重复数据--数据 元素、跟踪实体属性或选项集已经存在。
数据元素、跟踪实体属性或已存在的选项集。正如上文
正如上文关于解决冲突的部分所指出的，需要牢记的一个重要问题是，在决定对 DHIS2 中的元数据进行修改时
还需要考虑以不同方式与现有元数据和元数据相关联的其他文件和资源。
现有元数据和通过配置包导入的元数据。因此，解决重复问题
因此，解决重复问题不仅是一个 "清理数据库 "的问题，还要确保在清理过程中不
打破与其他系统集成的可能性、使用培训材料的可能性、打破 SOP 等。这在很大程度上取决于具体情况。
这在很大程度上取决于具体情况。

需要牢记的重要一点是，DHIS2 有一些工具可以掩盖元数据中潜在重复的复杂性。
元数据重复的复杂性。例如，在存在重复选项集的情况下，可以通过[共享]（#sharing）为用户组隐藏重复选项集。
共享]（#sharing）。

## 调整计划{ #adapting-the-program } 

程序导入后，您可能需要对程序进行某些修改。本地
可以进行的本地修改包括

- 向表单添加其他变量。
- 根据国家惯例修改数据元素/选项名称。
- 向变量和/或数据输入表单添加翻译。
- 根据当地案例定义修改指标

不过，如果您决定更改或删除任何包含的
表单/元数据。修改有可能会破坏功能，例如程序规则和程序
指标。

## 删除元数据{ #removing-metadata } 

为了保持实例整洁并避免出错，建议删除实例中不必要的元数据。
元数据。删除不必要的元数据需要 DHIS2 的高级知识和各种依赖关系。

