---
edit_url: "https://github.com/dhis2/dhis2-docs-implementation/blob/master/content/implementation/upgrades.md"
revision_date: '2025-06-02'
tags:
- 实施
---

# DHIS2 管理人员升级指南{ #dhis2-upgrade-guide-for-managers } 

使用受支持的 DHIS2 版本对确保系统安全非常重要。  DHIS2 的主要版本每年发布一次（通常在 4-5 月间）。在任何时候，最后 3 个主要版本都是受支持的，这意味着它们会收到安全补丁和错误修复。这意味着应计划至少每 3 年**进行一次主要版本升级，以确保使用受支持的版本。我们建议计划每年或每两年进行一次主要版本升级，因为这样可以确保更快地获得每个版本中包含的改进，并意味着每次升级过程中引入的更改更少。除主要版本升级外，还应定期应用次要更新和修复。

作为许多国家政府信息系统的重要组成部分，系统升级必须有周密的计划和实施，以避免中断。本节将从管理和实施的角度，从规划、预算、测试、培训等方面讨论与 DHIS2 升级有关的关键因素。本指南]（#techincal-upgrade-guide）介绍了系统管理员在服务器上进行实际软件升级时需要采取的具体步骤。

## 为什么要升级？ { #why-upgrade } 

升级 DHIS2 需要 DHIS2 核心团队和其他参与测试的人员投入时间和资源，而且确实存在一定的风险。不过，使用过时的软件也有风险。不断更新 DHIS2 意味着

* 接收最新的安全补丁和更新
* 受益于错误修复和性能改进
* 访问新功能
* 满足所需的依赖性

制定适当的升级计划和程序可降低相关风险，并在升级过程需要资源时提高能见度。我们强烈反对使用不支持的 DHIS2 版本，尤其是在采集任何个人/敏感数据的情况下。

## DHIS2 版本{ #dhis2-versions } 

DHIS2 核心版本遵循这一结构：

* 主要升级： 
    * 每年发布一次。
    * 涉及不同版本之间的重大变更（如从 2.38 到 2.39，或从 v41 到 v42）。
    * 包括数据库模式更改和新功能。
* 小规模升级： 
    * 定期发布维护版本。您可以查看即将发布的补丁的预计发布日期 [此处](https://dhis2.org/downloads/#next_patch)。
    * 包括错误修复和增量改进，将风险降至最低。 
* 热修复： 
    * 只有在必要时才释放。
    * 解决关键问题，可安全地应用于运行前一个补丁更新的系统。 

> **注**
>
> 从 v40 开始，主要版本的 2.前缀被 v（版本）取代。版本格式如下：`<major>.<patch>.<hotfix>`。这相当于传统的语义版本`<major>.<minor>.<patch>`。

### 持续发布的应用程序{ #apps-on-continuous-release } 

上述版本指的是 DHIS2 核心软件。DHIS2 附带一套*捆绑*应用程序，即软件自带的应用程序，无需从应用程序集线器等处手动安装。其中越来越多的应用程序还通过*持续发布*机制独立发布和更新。其中包括 "仪表盘"、"采集"、"数据录入 "和 "数据展示台 "等终端用户广泛使用的关键应用程序。每次发布 DHIS2 核心软件时，都会附带这些捆绑应用程序的最新版本。

持续发布的应用程序也可在应用程序管理应用程序中手动更新，*独立于 DHIS2 的核心升级。在这种情况下，手动更新的应用程序将优先于捆绑的应用程序。如果手动更新了应用程序，随后又更新了核心软件，则将使用手动安装的应用程序版本*，即使该版本比捆绑的版本旧*。如果删除了手动安装的版本，系统将返回到当前核心版本所包含的捆绑版本。

持续发布的应用程序为 DHIS2 开发人员和 DHIS2 实施人员提供了更大的灵活性，因为应用程序的更改和修正可以快速发布，实施人员可以通过 DHIS2 用户界面本身将这些更新安装到单个应用程序中。同时，从 DHIS2 升级和管理的角度来看，应用程序的持续发布也是一个复杂因素。应用程序的更新必须经过测试，在某些情况下可能会带来功能或用户界面上的变化，这些变化应告知用户。

> **注**
>
> 连续发布过程与许多人习惯使用的安卓或 iPhone 设备类似。安卓操作系统和 iOS 通常每年都会进行重大升级，而 Play Store/App Store 中的应用程序也可能随时更新。
> 然而，与智能手机应用程序不同的是，DHIS2 应用程序可以升级或*降级*到与当前核心版本兼容的任何版本。这就意味着，实施者可以完全控制新应用程序的推出，而不会被迫转向与核心升级捆绑在一起的版本。

