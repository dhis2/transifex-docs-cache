---
edit_url: "https://github.com/dhis2/dhis2-android-docs/blob/main/content/capture-app/settings.md"
revision_date: '2026-04-21'
tags:
- 用
---

# 设定值 { #capture_app_settings }

>在 3.3.0 版中，设置菜单经过重新设计，结构更简洁，导航体验更佳。这一更新使屏幕与之前版本中引入的设计变更保持一致，并为菜单承载未来的配置选项（如 2FA 相关设置）做好了准备。

设置窗口](resources/images/capture-app-image340.png){width=25%}。

## 同步数据{ #capture_app_settings_sync_data }

任何用户都可以从下拉列表中选择在应用程序中输入的数据是手动上传到服务器，还是计划每 15 分钟、1 小时或 1 天上传一次。默认情况下，应用程序将每 24 小时同步一次。这种同步包括新的和更新的事件和 TEI。

## 同步配置/元数据（在 3.4 中得到改进）{ #capture_app_settings_sync_metadata }

任何用户都可以从下拉列表中选择在应用程序中输入的数据是手动上传到服务器，还是按计划每天或每周上传到服务器。默认情况下，应用程序将每 24 小时同步一次。  该同步将更新网络中程序或配置的更改。

从 3.4 版开始，除了现有的时间间隔外，自动元数据同步现在可以每 6 或 12 小时运行一次，从而可以更及时地进行更新，并更好地与数据同步行为保持一致。

![](resources/images/capture-app-image366.png){width=25%}

## 同步参数{ #capture_app_settings_sync_parameters }

这些参数允许用户指定可存储在本地设备中的 TEI 和事件的最大数量。用户还可以指定限制适用于每个组织单位还是总计。单击 "重置为默认值 "可将数值设置为默认值。

> **注** 
>
> 同步数据、同步配置和同步参数可以使用 Android 设置 Web 应用程序进行覆盖，详见[特定部分][#capture_app_andoid_settings_webapp_synchronization][#capture_app_andoid_settings_webapp_synchronization]。
>
>

## 保留值{ #capture_app_settings_reserved_values }

这将指定设备中可用的预留 ID 数量，并允许您重新填充。

## 打开同步错误日志{ #capture_app_settings_errors_log }

同步错误日志会提供有关错误的详细信息，并准备与管理员共享。

## 导出数据库{ #capture_app_settings_export_db }

用户可以导出本地数据库并与管理员共享，管理员可以导入数据库进行故障排除，并复制确切的环境（数据库、设备、配置）。导出的数据库是加密的，管理员需要用户凭据才能访问数据库。

![](resources/images/capture-app-image341.png){ width=25%}

在登录界面点击树形圆点菜单即可找到导入按钮。

![](resources/images/capture-app-image338.png){ width=25%}

## 删除本地数据{ #capture_app_settings_delete_localdb }
:   该操作将删除本地数据，而无需注销。

> **警告** 
>
> 如果之前未将更改同步到服务器，使用此功能可能会导致数据丢失。
>
>

## 重置应用程序数据和配置{ #capture_app_settings_sync_metadata }
:   此操作将注销用户并删除所有数据和元数据，这意味着用户和服务器信息也将被删除。这与重置应用程序类似。

> **警告** 
>
> 如果之前未将更改同步到服务器，使用此功能可能会导致数据丢失。
>
>

## 短信设置{ #capture_app_settings_sms }

本部分允许检查或编辑与短信网关有关的参数。[更多信息请点击此处。](https://docs.dhis2.org/en/develop/developing-with-the-android-sdk/sms-module.html#android_sdk_sms_module)

## 软件更新{ #capture_app_settings_software_update }

该功能使实施管理员能够从 DHIS2 用户网络界面管理和控制 Android 应用程序的版本，从而更轻松地管理应用程序的更新并确保与 DHIS2 系统的兼容性。管理者可以上传所需的版本，当用户不在上次更新的版本中时，会收到更新提示信息。版本管理通过新的网络应用程序进行。

![](resources/images/capture-app-image229.png){ width=25%}

