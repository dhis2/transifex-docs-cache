---
edit_url: "https://github.com/dhis2/dhis2-docs/blob/master/src/implementation/data-elements-and-custom-dimensions.md"
revision_date: "2021-06-14"
---

# 数据元素和自定义维度 { #data-elements-and-custom-dimensions }

本章首先讨论系统的一个重要构建块：数据元素。其次，它讨论了类别模型以及如何使用它来实现用于数据存储的高度定制的元数据结构。

## 资料元素 { #data-elements }

数据元素和组织单位是 DHIS2 数据库最重要的组成部分。它代表了 "什么 "维度，解释了正在收集或分析的内容。在某些情况下，这被称为指标，但在 DHIS2 中，数据收集和分析的元数据元素被称为数据元素。数据元素通常代表对某些事件的计数，其名称描述了被计数的内容，如 "卡介苗剂量 "或 "疟疾病例"。在收集、验证、分析或展示数据时，数据元素或用数据元素构建的表达式描述了数据登记的现象、事件或病例。因此，数据元素对系统的方方面面都很重要，不仅决定了如何收集数据，更重要的是决定了如何在数据库中表示数据以及如何分析和展示数据。

设计数据元素背后的一个重要原则是将数据元素视为现象或事件的独立描述，而不是数据输入表单中的字段。每个数据元素都独立存在于数据库中，完全脱离并独立于集合形式。重要的是要考虑到数据元素直接在报告、图表和其他数据分析工具中使用，其中任何给定数据输入表单中的上下文都是不可访问或相关的。换句话说，必须能够仅通过查看数据元素的名称来清楚地识别数据元素代表什么事件。基于此，可以得出一条经验法则，即数据元素的名称必须能够独立存在，并且在其集合形式的上下文之外也能够描述数据值。

例如，当在捕获免疫数据的数据输入表单、捕获疫苗库存的表单以及门诊数据的表单中看到名为“疟疾”的数据元素时，该数据元素可能会很简洁。然而，当在数据输入表单的上下文之外查看报表时，无法确定该数据元素代表什么事件。如果数据元素被称为“疟疾病例”、“收到的疟疾库存剂量”或“给予的疟疾剂量”，那么从用户的角度来看，报告试图表达的内容就会很清楚。在这种情况下，我们正在处理具有完全不同语义的三个不同数据元素。

## 类别和自定义维度 { #categories-and-custom-dimensions }

数据捕获的某些要求需要对描述正在计数的事件的维度进行细粒度细分。例如，人们希望收集按性别和年龄组细分的“疟疾病例”数量，例如“女性”、“男性”和“\< 5 years” and “\> 5 年” 。其特点是，通常会针对许多“基本”数据元素重复进行细分：例如，人们希望将这种细分重新用于其他数据元素，例如“TB”和“HIV”。为了使元数据更加动态、可重用并且适合分析，将提到的疾病定义为数据元素并为细分属性创建单独的模型是有意义的。这可以通过使用类别模型来实现，如下所述。

类别模型具有三个主要元素，使用上面的示例可以最好地描述这些元素：

1.  类别选项，对应“女性”、“男性”和“\< 5 years” and “\> 5 年”。

2.  类别，对应于“性别”和“年龄组”。

3.  类别组合，在上面的示例中应命名为“性别和年龄组”，并分配上述两个类别。

该类别模型实际上是独立的，但在 DHIS2 中与数据元素松散耦合。在这方面松耦合意味着数据元素和类别组合之间存在关联，但是这种关联可以随时改变而不会丢失任何数据。然而，不建议经常更改此设置，因为它会降低数据的连续性，从而降低数据库的价值。请注意，一个类别中的类别选项数量或一个类别组合中的类别数量没有硬性限制，但是结构变得混乱和笨拙的地方存在自然限制。

现在可以使用一对数据元素和类别组合来表示任何级别的细分。重要的是要了解实际发生的情况是为数据分配了许多自定义维度。就像数据元素代表数据值的强制维度一样，类别也向其中添加自定义维度。在上面的例子中，我们现在可以通过DHIS2输出工具，对这些数据元素进行基于“性别”和“年龄段”的分析，就像基于数据元素、组织单位和时期进行分析一样。

该类别模型既可用于数据输入表单设计，也可用于分析和表格报告。出于分析目的，DHIS2 将自动为与类别组合相关的每个数据元素生成小计和总计。此计算的规则是所有类别选项的总和应为有意义的总数。上面的例子显示了这样一个有意义的总数，因为当总结“女性 \< 5 years”, “male \< 5 years”, “female \> 5 年”和“男性 \> 5 年”捕获的“疟疾病例”时，我们将得到总数“疟疾病例”。

出于数据捕获的目的，DHIS2 可以自动生成表格数据输入表单，其中数据元素表示为行，类别选项组合表示为列。在许多情况下，这会以最小的努力产生引人注目的形式。然而，有必要指出的是，这代表了一个两难的境地，因为这两个问题有时是不相容的。例如，人们可能希望通过使用不遵守有意义总计规则的类别来快速创建数据输入表单。然而，我们确实认为这是比维护两个独立且单独的数据输入和数据分析模型更好的选择。

关于类别模型的一个重要点是数据值被持久化并与类别选项组合相关联。这意味着从类别组合中添加或删除类别会使这些组合无效，并且必须执行低级数据库操作来纠正它。因此，建议仔细考虑需要哪些故障，并且不要过于频繁地更改它们。

## 数据元素组 { #data-element-groups }

数据元素的通用属性可以通过什么来建模
称为数据元素组。这些小组在
感觉他们的名字和成员资格都是由
用户。组对于浏览和显示相关数据非常有用，
并且还可以用于汇总为以下数据元素捕获的值
群组。组松散地耦合到数据元素，而不是束缚
直接指向数据值，这意味着可以对其进行修改和添加
在任何时间点都不会干扰底层数据。
