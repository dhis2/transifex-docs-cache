---
edit_url: "https://github.com/dhis2/dhis2-docs/blob/master/src/user/dhis2-glossary.md"
revision_date: '2021-07-13'
tags:
- Develop
- 使用
---

# DHIS2词汇表 { #dhis2_glossary }

## { #a }

聚合
：在 DHIS2 的上下文中，聚合是指数据元素如何
    结合在特定的层次关系中。作为一个
    例如，某个特定地区的所有卫生设施将
    对特定地区的总价值做出贡献
    问题。支持不同的聚合运算符
    DHIS2，例如 SUM、AVERAGE 和 COUNT。

分析
：分析是指处理和准备数据的过程
    已输入 DHIS2 的格式更
    适用于检索指标和汇总数据。当数据为
    输入到 DHIS2 中，它以优化的格式存储
    写入数据。然而，当数据需要被处理成
    指标或汇总（例如从几个月到几个季度），它更多
    有效地以不同的格式转换和存储这些数据
    它针对只读操作进行了优化。的分析系统
    DHIS2 被分析应用程序广泛使用（GIS、数据透视表、
    事件报告等）。

    重要的是要记住，因为数据已被
    输入 DHIS2 必须处理成分析格式，
    分析应用程序中出现的数据仅代表数据
    上次运行分析时系统中存在该信息。如果
    之后输入数据，需要运行分析
    再次让这些数据出现在分析应用程序中。

汇总数据
：在 DHIS2 上下文中，聚合数据是指任一数据
    来自其他方面的要素或指标
    分层数据源。例如，汇总设施数据
    将由所有患者的总和得出
    前往该设施进行特定服务。聚合区
    数据将来自所有设施的总计
    包含在特定区域中。

应用程序接口
：应用程序编程接口是如何规范
    不同的软件组件应该相互交互。这
    DHIS2 API（或 WebAPI）可用于将 DHIS2 与其他接口连接
    软件，用于构建报告或自定义数据输入表格。

批准
：批准可用于控制的可见性和可编辑性
    数据。当数据从最低报告级别提交时，可以
    并经上一级批准。该批准有两个作用：

    1. 无法再在数据输入屏幕中编辑数据
        较低的水平。

    2. 根据已启用的系统设置，
        数据将在审批级别可见。

    例如，数据在设施级别输入，并且
    提交审批。一旦数据被批准
    区级数据将被锁定在数据输入中
    设施级别的屏幕。它也将在
    向地区用户提供分析应用程序。

## B { #b }

双月刊
：指两个月期间，如1月1日至2月28日。

## C { #c }

类别
：类别是类别选项组。它们用于
    组合来分解数据元素。类别通常是
    单一类型的概念，例如“年龄”或“性别”。

品类组合
：类别组合用于分解数据元素。作为一个
    例如，数据元素“疟疾确诊病例数”
    可以细分为以下几类：“年龄”和
    “性别”。反过来，每个类别又包含几个
    类别选项，例如性别为“男性”和“女性”
    类别。类别组合可以由一个或多个组成
    类别。

类别组合选项
：类别组合选项由所有类别动态组成
    构成类别的类别选项的不同组合
    组合。例如，两个类别“性别”和“年龄”可能
    有“男性”或“女性”和“<5 years" or ">5 年”等选项。
    类别组合选项将包括：

    - 男性 <5 岁
    - 男性 >5 岁
    - 女性 <5 岁
    - 女性 >5 岁

类别选项
：类别选项是分组为的原子元素
    类别。

逗号分隔值
：逗号分隔值是存储在一个表格中的一系列表格数据
    纯文本格式。它们通常与 DHIS2 一起使用来导出和
    导入数据值。

## D { #d }

数据字典
：可以交换的数据元素和指标的集合
    与其他 DHIS2 系统。通常用于定义一组数据
    设置 DHIS2 系统时的要素和指标。

数据交换格式
：在 DHIS2 上下文中，“数据交换格式”是指 XML
    允许在之间传输数据和元数据的模式
    断开连接的 DHIS2 实例以及不同实例之间的连接
    支持 DXF 架构的应用程序。

数据库
：DHIS2 中的一组数据库表，包含已处理的数据
    聚合生成的元素和指标值
    规则和计算的数据元素和指标公式。数据库
    表格用于分析和报告生成。通常，用户
    不应该直接使用未聚合的数据值，而是
    使用数据集市导出产生的值进行分析。

数据元素
：数据元素是 DHIS2 的基本构建块。它是一个
    具有明确含义的数据原子单位。本质上它是一个
    实际观察到或记录的数据值
    进一步以多个维度为特征。举个例子
    数据元素“完全免疫儿童的数量”是指
    接受这项特定服务的儿童数量。数据
    元素总是与一个时期以及一个组织联系在一起
    单元。它们可以选择性地链接到其他维度。

数据元素组
：数据元素组用于对多个数据元素进行分类
    根据一个共同的主题，例如“免疫”或“ART”。
    通常，它们在报告和分析期间使用，以允许
    相关数据元素一起分析。

数据元素组集
：数据元素组用于对多个数据元素进行分类
    分组为一个共同的主题。

方面
：维度用于在分析过程中对数据元素进行分类。
    维度提供了一种根据以下条件对数据进行分组和筛选的机制
    共同特征。通常，相关数据元素可以是
    在分析过程中使用维度进行聚合或过滤。
    维度可以是层次结构的成员。例如“时期”
    维度可以细分为“日-\>月-\>季度-\>年”。

DXF

## H { #h }

健康管理信息系统
：通常是一个电子数据库系统，用于记录
    关于服务提供、疾病发生率、人类
    用于评估性能的资源数据和其他信息
    卫生服务的提供。通常，HMIS 不包含
    电子病历系统的高度详细数据或
    个体患者数据。

## 我{ #i }

指标
：指标的除数。可以由多个数据组成
    使用指标公式的元素。

    \[
    指标 = {\frac{分子}{分母}}
    \]

    这显然是一个非常普遍的例子。分子和
    指标本身可以由各种数据元素组成，
    因子和四个基本操作数（加法、乘法、
    除法和减法）。

## N { #n }

分子
：指标的红利。可以由多个数据组成
    使用指标公式的要素和因素。

## O { #o }

组织单位
：组织单位通常是地理单位，存在
    在一个层次结构内。举个例子，在美国，“乔治亚州”
    将被视为组织级别的组织单位
    的“国家”。组织单位也可用于指定
    行政单位，例如医院内的病房。这
    组织单位维度本质上指定*其中*a
    出现特定数据值。

组织单位级别
：指组织层次结构中的级别。通常，
    国家/地区有不同级别的管理，例如 1) 国家/地区
    2) 州 3) 县 4) 卫生设施。在 DHIS2 的背景下，
    卫生设施通常是最低级别的机构。数据是
    从最低的组织级别到最高的组织级别向上聚合。

## P { #p }

时期
：周期是由开始日期组成的特定时间间隔
    和结束日期。例如“2011 年 1 月”指的是时间
    时间间隔为2011年1月1日至2011年1月31日。

## U { #u }

唯一标识符
：唯一标识符 (UID) 是一系列半随机的字母和
    DHIS2 用于识别特定资源的编号。 UID 开始
    带有一个字母，后跟 10 个字母或数字。

