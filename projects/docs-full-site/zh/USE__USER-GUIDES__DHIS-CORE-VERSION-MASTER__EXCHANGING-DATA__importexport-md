---
edit_url: "https://github.com/dhis2/dhis2-docs/blob/master/src/user/importexport-app.md"
revision_date: '2026年9月2日'
tags:
- Develop
- Use
---

# 导入/导出应用 { #import_export }

在初级卫生系统中，HMIS 通常是一个分布式应用项目。
在基层卫生系统中，HMIS 通常涉及分布式应用项目，即同一项目在不同的地理位置（初级保健中心
在不同的地理位置（初级保健中心、社区保健中心、医院、地区和州）运行同一个应用项目。
其中许多地理位置都没有互联网连接，因此只能离线工作。
因此，它们只能离线工作。在某些时候（通常是在区
通常在地区一级）需要同步数据，以便为特定地理区域建立一个综合数据库。
在某些时候（通常是在地区一级），数据需要同步，以便为特定地理区域建立一个综合数 据库。为此
为此，重要的是能够从一个地点（离线工作，例如在医疗机构）导出数据。
为此，重要的是能够从一个地点（离线工作，如卫生机构一级）导出数据，并导入到另一 个地点（如地区一级）。
导入另一个地点（如地区一级）。
因此，这种导出和导入功能是 HMIS 的一项重要功能。
这一功能也在一定程度上帮助我们克服了对互联网的依赖。
在没有网络连接的地方，可通过 USB 密钥传输数据更新；在有网络连接的地方，可通过电子邮件传输数据更新。
传输数据更新，或在互联网连接有限的地方通过电子邮件传输数据更新。
连接。DHIS2 提供了强大的导入导出功能，以满足这些需求。
满足这些需求。

要访问导入/导出应用，请在顶部标题栏中搜索
进出口。导入/导出应用程序提供了许多服务详细信息
可以在下面找到。

![](resources/images/import_export/overview.png)


## 汇入资料 { #importing_data }

### 导入进度记录器 { #import_progress_logger }

无论您导入什么数据（"数据"、"事件"、"组织单位几何图形"、"元数据 "或 "跟踪实体 "数据），都可以通过页面顶部的 "任务摘要 "查看导入进度。

### 导入摘要 { #metadata_import_summaries }

导入请求完成后，我们会在导入表单上方显示导入摘要。
导入表格。任何冲突或错误都会显示在导入主摘要下的表格中。
下的表格中显示。

![](resources/images/import_export/import_summary.png)

### 元数据导入 { #metadata_import }

可以从侧边栏单击来访问元数据导入
元数据导入。

![](resources/images/import_export/metadata_import.png)

1.  选择要上传的文件

2.  请选择格式：_JSON_ 或 _CSV_

3.  选择适当的设置：

    - 标识符（是否根据UID或代码与现有元数据进行匹配）
    - 导入报告模式（导入完成后报告的详细程度）
    - 导入策略（应如何导入值）
    - 原子模式（控制导入中的某些对象无效时会发生什么）

4.  如果要调整以下一项或多项，请单击**高级选项**。 
    导入前请进行以下设置：

    - 清空模式（控制何时清空内部缓存）
    - 跳过共享（导入时是否包含或忽略共享）
    - 跳过验证（是否在导入过程中跳过验证检查）
    - Async（导入是否以异步方式进行）

5.  点击**“开始导入”**按钮，系统将上传文件并开始
    导入过程。

> **提示**
>
> **强烈建议在导入数据前使用“模拟运行”选项**进行测试，
> 以确保您能够掌控对元数据的任何更改，
> 并检查数据元素或
> 组织单位名称是否存在不同步的问题

> **注意**
>
> 如果某个组织单位（例如 `Nduvuibu MCHP` 包含对 ID 为 `aaaU6Kr7Gtpidn` 的对象的未知引用，这意味着 ID 为 `aaaU6Kr7Gtpidn` 的对象既不在您导入的文件中，也未在现有数据库中找到。
>
> 您可以通过**标识符**选项进行控制，以指定是否允许导入包含此类无效引用的对象。如果您选择导入无效引用，则稍后需要在 DHIS2 中手动更正该引用。

#### DXF2中的匹配标识符 { #matching_identifiers_in_dxf2 }

DXF2 格式目前支持对两个标识符进行匹配，即
DHIS2 内部标识符（称为 UID），以及一个名为“代码”的
外部标识符。 当导入工具尝试搜索
引用（如上所述）时，它会首先检索 UID 字段，
然后检索 code 字段。这使您能够从旧系统
导入数据，而无需为每个元数据对象都提供 UID。 也就是说，如果您正在
从旧系统导入设施数据，可以完全省略 ID
字段（DHIS2 会为您自动填充），并将
旧系统自身的标识符填入 code 字段，该标识符
必须是唯一的。 这不仅适用于组织单位，
也适用于各类元数据，从而便于从其他系统导入数据。

### 资料汇入 { #import }

可以通过单击侧边栏上的数据来访问数据导入
进口。

![](resources/images/import_export/data_import.png)

1.  选择要上传的文件

2.  选择一种格式：_JSON_、_CSV_、_XML_、_ADX_ 或 _PDF_

3.  请为以下内容选择适当的设置：

    - 策略（如何导入值）
    - 预热缓存（通过使用临时缓存图来加快导入速度）
    - 跳过审核（禁用审核可加快导入速度）

4.  如果要调整以下一项或多项，请单击**高级选项**。 
    导入前请进行以下设置：

    - 数据元素ID方案
    - 组织单位ID方案
    - ID方案
    - 跳过现有检查（可提高性能，但仅建议用于空数据库或您确定数据不存在的情况）

5.  点击“**开始导入**”按钮，系统将上传文件并开始
    导入过程。

> **提示**
>
> **强烈建议您先使用空运行选项**进行测试
>导入数据；确保您可以控制自己的任何更改
>元数据，并检查数据元素不同步或
>组织单位名称

#### PDF资料 { #importPDFdata }

DHIS2支持以PDF格式导入数据。这可以用来
导入由离线PDF数据输入表单生成的数据。请参阅
**数据集管理**一节中介绍了如何生成PDF的详细信息
可用于离线数据输入的表格。

要导入 PDF 数据文件，请在
侧边菜单中找到 _PDF 数据导入_ 选项。上传已填写好的 PDF 文件，然后点击 _导入_。

### 事件汇入 { #event_import }

可通过侧边栏点击“活动
导入”来访问“活动导入”功能。

![](resources/images/import_export/event_import.png)

1.  请选择格式：_JSON_ 或 _CSV_。

2.  如果要调整以下一项或多项，请单击**高级选项**。 
    导入前请进行以下设置：

    - 数据元素ID方案
    - 组织单位ID方案
    - ID方案

3.  点击**“开始导入”**按钮，系统将上传文件并开始
    导入过程。

### Earth Engine 导入{ #ee_import }

可通过侧边栏点击“Earth Engine
导入”来访问 Earth Engine 导入功能。

使用 Google 地球引擎从 WorldPop 导入高分辨率人口数据。使用此导入项目需要 [Google
Earth Engine 帐户](https://docs.dhis2.org/en/topics/tutorials/google-earth-engine-sign-up.html) 才能使用此导入项目。

![](resources/images/import_export/ee_import.png)

#### 选择要导入的 Earth Engine 数据{ #select-which-earth-engine-data-should-be-imported } 

表单的第一部分用于配置要导入的 Earth Engine 数据。

1. 选择要导入的 Earth Engine 数据集。目前可选的选项包括 *人口* 和 *人口年龄组*。

2. 选择数据集后，必须选择一个时间段。每次只能导入一个时间段。

3. 选择数据的四舍五入方式。默认情况下，数据不会进行四舍五入。

4. 选择要导入数据的组织单位。如果选择设施级组织单位，则必须为这些设施选择相应的几何形状。如果设施没有关联的几何形状，Earth Engine 将无法确定人口数量。

![](resources/images/import_export/ee_ou_associated_geometry.png)

#### 选择要导入的数据元素，将 Earth Engine 数据导入{ #select-the-data-elements-to-import-the-earth-engine-data-into } 

配置好 Earth Engine 数据集后，您需要选择将数据导入到哪个数据元素中。对于包含细分组的数据集（例如“人口年龄组”），DHIS2 数据元素中的细分必须以类别下拉列表的形式呈现，且其分类与 Earth Engine 数据集的细分组相匹配。

![](resources/images/import_export/ee_group_coc_mapping.png)

> **配置用于导入 Earth Engine 的数据元素**
>
> 在配置将包含 Earth Engine 数据的 DHIS2 数据元素时，如果您计划将数据导入多个组织单位层级，请确保在数据元素配置中将这些层级添加为“聚合层级”。
>
> 部分 Earth Engine 数据集包含细分组，因此必须在 DHIS2 数据元素中配置相应的类别选项组合。例如，“人口年龄组”数据集按性别（男性、女性）和 5 岁年龄组进行细分。
>
> 在 DHIS2 中，这意味着您必须设置“男性/女性”类别和“5岁年龄组”类别（<1yr, 1-4yr, 5-9yr, 10-14yr... 80+yr). These are combined into a category combination.
>> 专业提示：要自动将类别选项组合与 Earth Engine 的细分组匹配，请为每个类别选项组合添加一个代码，该代码需与 Earth Engine 组名相对应。 例如，在“人口年龄组”中，各组名称分别为：F_0、F_1、F_5……、M_0、M_1、M_5……

#### 运行导入脚本{ #run-the-import } 

选定数据元素和类别选项组合后，“预览”按钮将变为可用状态。在确认要导入的数据后，您可以先进行一次模拟操作，然后执行实际导入。

![](resources/images/import_export/ee_data_preview.png)

### 组织单元几何形状导入{ #geometry_import }

通过侧边栏点击**组织单位几何数据导入**即可访问。

支持两种几何格式：**GeoJSON** 和 **GML**。推荐使用 GeoJSON 格式，且这是唯一可用于导入关联几何对象（例如流域）的格式。 对 GML 的支持仅限于 2.0 版本，此功能仅为方便起见而保留，未来版本中不计划对其进行扩展或增强。

另请参阅 [地图配置指南](https://docs.dhis2.org/en/use/user-guides/dhis-core-version-master/configuring-the-system/maps.html) ，了解如何在导入前准备源数据（转换为 GeoJSON/GML 格式、指定所需的坐标参考系统、简化复杂几何图形），以及如何排查常见的导入错误。

#### GeoJSON 导入{ #geojson_import }

![](resources/images/import_export/geojson_import.png)

1. 上传一个 GeoJSON 格式的文件。默认情况下，导入过程会将每个 GeoJSON 要素的 `id` 与相应的组织单位 ID 进行匹配。

2. 在导入之前，强烈建议勾选 **模拟运行** 以预览更改而不实际应用——这样可以确保您能够掌控对组织单元几何形状所做的任何更改。

3. 点击**“开始导入”**以上传文件并启动导入流程。

##### 可选设置{ #optional-settings } 

这些设置会改变导入的行为，并可根据需要与上述步骤结合使用：

- **按其他属性进行匹配：** 默认情况下，匹配是基于组织单位 ID 进行的，使用 GeoJSON 要素的顶级 `id` 成员（而非 `"properties"` 内的属性）。若要改用其他属性进行匹配，请勾选 **将 GeoJSON 属性与组织单位方案进行匹配**。 输入 GeoJSON 属性名称，并选择要进行匹配的组织单位 ID 方案（**ID**、**Code** 或 **Name**）。

- **导入为关联几何：**若要将 GeoJSON 要素导入为关联几何（例如集水区），而非组织单位的主几何，请勾选 **导入为关联几何**，并选择用于导入数据的几何属性。 这需要为“组织单位”类型分配一个类型为**GeoJSON**的属性，该属性可在“元数据管理”应用（原“维护”应用）中创建。

##### GeoJSON 结构示例{ #geojson-structure-examples } 

默认匹配，其中特征的顶级 `id` 指代组织单位 ID：

```json
{
  "type": "FeatureCollection",
  "features": [
    {
      "type": "Feature",
      "id": "O6uvpzGd5pu",
      "geometry": { "type": "Point", "coordinates": [12.34, 56.78] }
    }
  ]
}
```

根据某个属性（例如 Code）进行匹配，此时该标识符应放置在 `"properties"` 内：

```json
{
  "type": "FeatureCollection",
  "features": [
    {
      "type": "Feature",
      "properties": { "code": "OU1_CODE" },
      "geometry": { "type": "Point", "coordinates": [12.34, 56.78] }
    }
  ]
}
```

#### GML 导入{ #gml_import }

![](resources/images/import_export/gml_import.png)

1. 请上传**GML 2.0**格式的文件。仅支持 GML 2.0 格式。

2. 在导入之前，强烈建议勾选 **模拟运行** 以预览更改（不会实际应用这些更改）——这样可以确保您能够掌控对组织单元几何形状所做的任何更改。

3. 点击**“开始导入”**以上传文件并启动导入过程。

> **注意：** 出于方便起见，GML 格式仍被保留，但今后将不再积极进行开发。**GeoJSON 是推荐的格式**，用于导入组织单位的几何数据。

### 受监控实体的导入{ #tei_import }

可通过侧边栏访问“被追踪实体导入”功能，
方法是点击“被追踪实体导入”。

![](resources/images/import_export/tei_import.png)

1.  选择一个 JSON 文件进行上传

1.  请为以下项目选择适当的设置：

    - 标识符（是否根据 UID 或代码与现有元数据进行匹配）
    - 导入报告模式（导入完成后报告的详细程度）
    - 预热模式（控制系统在导入前如何预加载/缓存元数据）
    - 导入策略（应如何导入值）
    - 原子模式（控制导入中的某些对象无效时应如何处理）
    - 合并模式（合并对象时的策略）

1.  如果要调整以下一项或多项，请单击**高级选项**。 
    导入前请进行以下设置：

    - 清空模式（控制何时清空内部缓存）
    - 跳过共享（导入时是否包含或忽略共享）
    - 跳过验证（导入时是否跳过验证检查）
    - 包含策略（控制要包含哪些属性）
    - 数据元素ID方案
    - 组织单位ID方案
    - ID方案

1.  点击**“开始导入”**按钮，系统将上传文件并开始
    导入过程。

> **提示**
>
> **强烈建议在导入数据前使用“模拟运行”选项**进行测试，
> 以确保您能够掌控对
> 所跟踪实体的任何更改。

## 汇出资料 { #exporting-data } 

### 元数据导出 { #metadata_export }

单击侧边栏上的，可以访问元数据导出
元数据导出。

![](resources/images/import_export/metadata_export.png)

1.  选择您要导出的对象列表。

2.  选择格式：*JSON*

3.  请选择压缩类型：_zip_、_gzip_ 或 _未压缩_

4.  决定是否勾选“跳过共享和访问设置”

5. 在“高级选项”中，可以修改“包含策略”（用于控制包含哪些属性）

6.  点击**导出元数据**，这将打开一个新的网络浏览器窗口
    这样你就能获得一个文件，可以将其下载到本地计算机上。

### 具有依赖项的元数据导出 { #metadata_export_dependencies }

具有依赖项的元数据导出使您可以为以下内容创建罐头导出
元数据对象。这种类型的导出将包括元数据对象
和元数据对象的相关对象；即元数据
与主要对象一起属于

表：对象类型及其依赖关系

| CSV元数据导入 { #webapi_csv_metadata_import }           | 导出中包含的依赖项                                                                                                                                                                                                                                                                                                                                                           |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **数据集**        | 数据元素<br> <br> 部分<br> <br> 指标<br> <br> 指标类型<br> <br> 属性<br> <br> 数据输入表单<br> <br> 图例集<br> <br> 图例<br> <br> 类别组合<br> <br> 类别<br> <br> 类别选项<br> <br> 类别选项组合<br> <br> 选项集                                                                                              |
| 程式             | 数据输入表<br> <br> 跟踪实体<br> <br> 项目阶段<br> <br> 项目属性<br> <br> 项目指标<br> <br> <br> <br> 项目规则<br> <br> 项目规则动作<br> <br> 项目规则变量 <br> 项目属性<br> 数据元素<br> <br> 类别组合<br> <br> 类别<br> <br> 类别选项<br> <br> 类别选项组合<br> <br> 选项集 |
| 不 | 类别组合 <br> <br> 类别 <br> <br> 类别选项 <br> <br> Category 选项组合 <br><br>属性                                                                                                                                                                                                                                                                 |
| 仪表板            | 仪表板项目<br> <br> 图表<br> <br> 事件图表<br> <br> 数据透视表<br> <br> 事件报告<br> <br> 地图<br> <br> 报告<br> <br> 资源                                                                                                                                                                                                                                             |
|     POST / api / metadata?classKey = CLASS-KEY  | 数据元素<br> <br> 类别组合<br> <br> 类别<br> <br> 类别选项<br> <br> 类别选项组合<br> <br> 选项集<br> <br> 属性<br> <br> 图例集<br> <br> 图例                                                                                                                                                                                   |
| 选项集           | Validation rule                                                                                                                                                                                                                                                                                                                                                                                    |

![](resources/images/import_export/metadata_dependency_export.png)

![](resources/images/import_export/metadata_dependency_export_object_types.png)

1.  选择对象类型：_数据集_、_程序_、_类别组合_、
    _仪表盘_、_数据元素组_ 或 _选项集_。

2.  选择一个对象。

3.  选择格式：*JSON*

4.  选择压缩类型：_Zip_、_GZip_ 或 _未压缩_

5.  点击 **导出元数据依赖关系**，这将打开一个新的
    一个网页浏览器窗口，它会提供一个文件供您下载到您的
    本地计算机。

### 资料汇出 { #data_export }

可以通过单击数据从侧边栏访问数据导出
出口。

![](resources/images/import_export/data_export.png)

1.  选择要导出的组织单位。

2.  如果希望导出内容包含该组织的下属单位，请勾选此选项
    在步骤 1 中选定的单位，还是仅手动选定的
    组织单位。

3.  选择要导出的数据集。

4.  设置开始和结束日期。

5.  选择一种格式：_JSON_、_CSV_、_XML_ 或 _ADX_

6.  选择压缩模式：**Zip**、**GZip** 或 **未压缩**

7.  如果要调整以下一项或多项，请单击**高级选项**。 
    导出前请进行以下设置：

    - 包括已删除
    - 数据元素ID方案
    - 组织单位ID方案
    - ID方案

8.  点击**“导出数据”**，这将打开一个新的网页浏览器窗口
    这样你就能获得一个文件，将其下载到本地计算机上。

### 活动导出 { #event_export }

通过单击事件可以从边栏中访问事件导出
出口。

![](resources/images/import_export/event_export.png)

您可以将事件或追踪器数据导出为 JSON 或 CSV 格式。

1.  选择一个组织单位。

1.  选择包含内容：

    - _已选中_：仅导出所选项的活动数据
      组织单位

    - _正下方_：导出事件数据，包括第一个
      以及选择范围内的组织单位层级
      作为所选组织单位本身。

    - _以下全部_：导出所有组织单位的事件数据
      在选项内部以及所选组织中
      该单元本身。

1.  选择一个项目和一个项目阶段（如果适用）。

1.  设置开始日期和结束日期。

1.  请选择格式：_JSON_ 或 _CSV_。

1.  选择一种压缩模式：_Zip_、_GZip_ 或 _未压缩_。

1.  如果要调整以下一项或多项，请单击**高级选项**。 
    导出前请进行以下设置：

    - 包括已删除
    - 数据元素ID方案
    - 组织单位ID方案
    - ID方案

1.  点击**“导出事件”**，这将打开一个新的网页浏览器窗口
    这样您就可以下载一个文件到本地计算机上。

### 受监控实体的导出{ #tei_export }

可通过侧边栏访问“已跟踪实体导出”功能，
方法是点击“已跟踪实体导出”。

![](resources/images/import_export/tei_export.png)

您可以将已跟踪的实体导出为 JSON 或 CSV 格式。

1.  选择应包含的组织单位。选择组织单位有三种模式：


    - _可访问_：选择与当前用户关联的数据视图组织单位

    - _捕获_：选择与当前用户关联的数据视图组织单位。

    - _手动选择组织单位_：手动选择组织单位。

1. 如果您选择“_手动选择组织单位_”，系统将显示更多选项：

    - _已选中_：仅导出所选数据
      组织单位

    - _正下方_：导出数据，包括第一个
      以及选择项内的组织单位层级
      作为所选的组织单位本身。

    - _以下全部_：导出所有组织单位的事件数据
      在选项内部以及所选组织中
      该单元本身。

1.  决定是按_项目_还是按_跟踪实体类型_进行筛选。

1. 确定要包含在导出中的状态

1. 确定要将哪些跟进状态包含在导出文件中

1.  请选择格式：_JSON_ 或 _CSV_。

1.  如果要调整以下一项或多项，请单击**高级选项**。 
    导出前请进行以下设置：

    - 按上次更新日期筛选
    - 按指派用户筛选
    - 包括已删除
    - 数据元素ID方案
    - 组织单位ID方案
    - ID方案

1.  点击**“导出已跟踪实体”**，这将打开一个新的
    一个网页浏览器窗口，它会提供一个文件供您下载到您的
    本地计算机。

## v41及以上版本中 Tracker 导入/导出功能的变更{ #v41_tracker_changes }

导入/导出应用项目已升级为使用[新的跟踪器 API](https://docs.dhis2.org/en/develop/using-the-api/dhis-core-version-master/tracker.html) 来导入和导出跟踪的实体和事件。废弃的跟踪器 API](https://docs.dhis2.org/en/develop/using-the-api/dhis-core-version-master/tracker-deprecated.html) 将在 v42 中移除，因此建议所有应用项目尽快升级。

这在受监控实体和事件的导入/导出体验的一致性和可靠性方面带来了诸多好处，例如更完善的验证、更完善的错误报告以及采用更可靠的作业调度工作流。

这些优点有几个重要的注意事项。首先，导出文件的新格式与以前版本的应用项目不兼容，因此 v41 的导出文件无法导入以前的版本，而以前版本的导出文件也无法导入 v41。我们还放弃了对 XML 格式的支持，只支持 JSON 和 CSV。有关这些更改的更多信息，请参阅 [Tracker API 文档](https://docs.dhis2.org/en/develop/using-the-api/dhis-core-version-master/tracker.html) 。

![v41 及更高版本中改进的错误报告](resources/images/import_export/v41_error_reports.png)


## 工作概述 { #job_overview }

您可以通过点击侧边栏中的
_职位概览_ 访问职位概览页面。

![](resources/images/import_export/job_overview.png)

在此页面上，您可以查看本会话中已启动的所有导入任务的
进度。您可以在左侧查看所有任务的列表，
并在右侧查看所选特定任务的详细信息。

### 按导入作业类型过滤 { #filtering-by-import-job-type } 

![](resources/images/import_export/job_overview_filter.png)

默认情况下，所有类型的职位都会显示在职位列表中，但您
可以通过点击职位列表上方的职位
类型筛选器，筛选出您感兴趣的类别。

### 重新创建上一份工作 { #recreating-a-previous-job } 

![](resources/images/import_export/job_overview_recreate.png)

如果您已从列表中
选定一个任务，则可通过点击页面底部的
_重新创建任务_ 按钮来重新创建之前运行过的导入任务。这将带您进入相应的导入
页面，并完全按照您选择要
重新创建的任务，自动填入所有表单详细信息。

## 方案 { #schemes } 

许多导入和导出页面中使用的各种方案
也被称为标识符方案，用于在导入过程中将元数据对象
映射到其他元数据，并在导出时将元数据作为
导出内容的一部分呈现。

表：可用值

| ID, UID       | Parameter name                                                                                                                                                                                                                                                                                                                                                         |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CODE      | 与 DHIS2 编码匹配,主要用于与外部系统交换数据。                                                                                                                                                                                                                                                                                                    |
| NAME         | 在 DHIS2 名称上匹配,请注意这使用的是可用的 *object.name*,而不是翻译后的名称。还请注意,名称并不总是唯一的,在这种情况下,不能使用它们。                                                                                                                                                                                                                                                                                          |
| ATTRIBUTE:ID         | 根据 DHIS2 名称进行匹配，请注意，此处使用的是 _object.name_ 中的可用名称，而非翻译后的名称。另请注意，名称并不总是唯一的，在这种情况下，则无法使用该名称。                                                                                                                                                                |
| 请注意,标识符方案不是一个独立的功能,需要与数据值导入、元数据导入和
GeoJson 导入等资源结合使用。 | 根据元数据属性进行匹配，该属性需要分配给您要进行匹配的类型，并且“unique”属性必须设置为_true_。 该功能的主要用途也是与外部系统交换数据，与 _CODE_ 相比，它具有一些优势，因为可以添加多个属性，因此可用于与多个系统进行同步。 |

### ID方案 { #id-scheme } 

该 ID 方案适用于所有类型的对象，但可能会被
更具体的对象类型所覆盖。

