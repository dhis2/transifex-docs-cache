---
edit_url: "https://github.com/dhis2/dhis2-docs/blob/master/src/sysadmin/reference/file-storage.md"
revision_date: '2025-02-27'
tags:
- 系统管理
---

# 文件存储配置 { #install_file_store_configuration } 

DHIS2 能够捕获和存储文件。默认情况下，文件将
文件将存储在运行 DHIS2 的服务器的本地文件系统中，位于
目录下。 

目录文件可通过 `dhis.conf.` 中的 `filestore.container` 属性更改。
您也可以将 DHIS2 配置为在云存储提供商上存储文件。
存储提供商上存储文件。目前支持 AWS S3 或与 S3 兼容的对象存储。
支持。 

要启用云存储，必须在 `dhis.conf` 文件中定义以下附加属性
文件中定义以下附加属性：

属性
# 文件存储提供程序。目前支持 "filesystem"（默认）、"aws-s3 "和 "s3"。
filestore.provider = "aws-s3

# 本地文件系统外部目录或 AWS S3 或 S3 API 中的存储桶中的目录
filestore.container = files

# 以下配置仅适用于云存储（提供商 'aws-s3' 或 's3')

# 数据中心位置。可选，但出于性能考虑建议使用。
filestore.location = eu-west-1

# AWS S3 或 S3 API 的用户名/访问密钥
filestore.identity = xxxx

# 用于 AWS S3 或 S3 API 的密码/秘钥（敏感）
filestore.secret = xxxx
```

要在兼容 S3 的对象存储中启用存储，必须在 dhis.conf 文件中定义以下附加属性：

属性
# 文件存储提供程序。目前支持 "filesystem"（默认）、"aws-s3 "和 "s3"。
filestore.provider = 's3'

# 目录位于本地文件系统的外部目录或 AWS S3 的存储桶中
filestore.container = files

# 以下配置仅适用于云存储（提供商为'aws-s3'或's3）

# 可访问 S3 兼容 API 的 URL（仅适用于提供商 "s3）
filestore.endpoint = http://minio:9000 

# 数据中心位置。可选，但出于性能考虑建议使用。
filestore.location = eu-west-1

# AWS S3 或 S3 API 的用户名/访问密钥
filestore.identity = xxxx

# 用于 AWS S3 或 S3 API 的密码/秘钥（敏感）
filestore.secret = xxxx
```

> **注**
> 
> 如果您在 `dhis.conf` 中配置了云存储，您上传的所有文件
> 或系统生成的文件都将使用云存储。

这些配置只是示例，应根据您的需要进行更改。对于
对于生产系统，文件存储的初始设置应仔细考虑
对于生产系统，文件存储的初始设置应仔细考虑，因为在存储提供商之间移动文件，同时保持
数据库引用的完整性可能会很复杂。请记住
文件存储的内容可能既包含敏感信息，也包含不可分割的信息。
在生产实施中，建议保护文件夹的访问权限并确保备份计划到位。
建议在生产实施过程中做好备份计划。

> **注**
>
> AWS S3 和 S3 兼容对象存储是唯一受支持的云提供商
> 但可以添加更多提供商。如果您需要
> 请告诉我们。
>

