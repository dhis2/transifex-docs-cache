---
edit_url: "https://github.com/dhis2/dhis2-emis-docs/blob/main/SEMIS/SEMIS_ConfigManual.md"
revision_date: '2026-04-02'
tags:
- 实施
---

# SEMIS（学员跟踪系统）--配置手册{ #semis-learner-tracker-configuration-manual } 

# COORDINATE

SEMIS 应用程序**提供了在 DHIS2 中使用**跟踪程序**管理学生和教职员工信息的功能。除操作数据录入外，该应用程序还包括内置配置选项，允许管理员管理系统设置和元数据。

与使用单独配置应用程序的系统不同，SEMIS 将配置直接集成在**SEMIS 应用程序界面**内。管理员可通过选择 SEMIS 应用程序中的**配置选项**来访问配置功能。

这种集成式设计可确保系统管理员在配置系统的同时，仍能在用于操作工作流程的相同应用环境中进行配置。

通过 SEMIS 配置界面，管理员可以

* 激活或关闭功能模块  
* 配置程序阶段和元数据  
* 地图选项集和数据元素  
* 管理与学年和学校日历相关的系统设置

# 用户角色和访问要求。  { #user-roles-and-access-requirements } 

访问 SEMIS APP 配置选项卡通常仅限于具有**管理权限**的用户。

| 用户角色 | 责任 |
| :---- | :---- |
| 系统管理员 | 配置元数据、程序和模块激活 |
| 实施专家 | 配置跟踪器工作流程和数据结构 |

用户必须在 DHIS2 中拥有以下权限：

* 进入**SEMIS 应用程序**  
* 配置**跟踪程序的权限**  
* 访问**维护应用程序**元数据配置


# 如何访问 SEMIS 配置{ #how-to-access-semis-configuration } 

要配置 SEMIS 设置，管理员必须访问 SEMIS 应用程序中的配置部分。

注意：在 DHIS2 中配置学生和教职员工追踪程序后，使用 SEMIS 配置应用程序。

## 访问配置的**步骤**{ #steps-to-access-configuration } 

1. 使用管理员凭据登录**DHIS2**。  
2. 从 DHIS2 导航栏打开**应用程序菜单**。  
3. 选择 **SEMIS 应用程序**。

![](SEMISconfigimages/image2.png)

4. 在 SEMIS 界面的右侧面板上有**配置**选项。  
5. 在 "配置 "下有两个选项：  
* 校历  
* 配置

配置界面显示所有可用模块，并将其归类：

* **学生单元**  
* **工作人员模块**

每个模块都可以根据系统要求进行激活和配置。

## ** 建议的配置步骤：**{ #recommended-steps-for-configuration } 

1. 配置校历  
2. 配置学生模块屏幕  
3. 配置工作人员模块屏幕  
4. 验证工作流程，如注册、考勤、最终结果、转学流程。

# 模块激活{ #module-activation } 

SEMIS 配置界面包括切换开关，允许管理员启用或禁用特定模块。

切换状态： 

| Additionally, the following endpoint will return the import summary of the import job. This import
summary will only be available after the import has completed: | 意义 |
| :---- | :---- |
| 绿色![](SEMISconfigimages/image18.png) | 模块已激活并可供使用 |
| 灰色![](SEMISconfigimages/image13.png)| 模块处于非活动状态，隐藏在业务工作流程中 |

管理员应只激活当前**教育系统实施所需的模块**

**例如：**

* 如果以数字方式跟踪学生出勤情况，则应激活学生出勤模块。  
* 如果没有收集社会经济信息，社会经济模块可能仍处于不活动状态。

激活模块可以

* 数据输入接口  
* 计划阶段工作流程。  
* 相关分析和报告功能

# 校历配置{ #school-calendar-configuration } 

校历**允许管理员在 SEMIS 中定义学年结构。它包括学年、学期、上课日和非上课日的配置。  
正确的配置可确保**考勤跟踪、学业报告和分析**等模块根据定义的学年时间表正确运行。

校历有助于系统的运行：

* 建立**学年背景**  
* 确定**个有效出勤日**  
* 控制 **默认筛选器和报告期**  
* 保持**学校运行时间表**的一致性

## **获取校历**{ #accessing-the-school-calendar } 

脚步

1. 在 DHIS2 中打开 **SEMIS 应用程序**。  
2. 导航至配置区域内的**校历部分**。  
3. 页面显示**现有学年**列表。

界面通常显示

* 学年 姓名  
* 学年代码  
* 开始和结束日期  
* 操作（查看详情、编辑、删除）

![](SEMISconfigimages/image1.png)

## **创造新学年**{ #creating-a-new-academic-year } 

在学校开始记录出勤或学业数据之前，管理人员必须创建一个新学年。

脚步

1. 点击 **新学年选项**。  
2. 将出现一个输入学年详细信息的表格，即姓名和代码
3. 点击保存
4. 然后点击方框中的**笔图标**。
5. 输入以下信息。

| The manual merge is suitable when there are resolvable conflicts or when not all the data needs to
be moved during the merge. For example, if an attribute has different values in both tracked
entities , the user can specify whether to keep the original value or move over the duplicate's
value. Since the manual merge involves the user explicitly requesting to move data, there are some
additional checks:  | Parameter name |
| :---- | :---- |
| 类型 | 学年全称（例如，2026 学年 （- 2027\) |
| zscore | 系统短代码（如 AY2026） |
| 开始日期 | 学年正式开始日期 |
| 结束日期 | 学年正式结束日期 |

6. 保存后，学年将出现在学年列表中。

   ![](SEMISconfigimages/image4.png)
   ![](SEMISconfigimages/image19.png)
   ![](SEMISconfigimages/image20.png)


## **查看和配置学年详情**{ #viewing-and-configuring-academic-year-details } 

您需要配置 
1. 一般详情
2. 学校用语
3. 非上课日

### **在 "一般详情 "下配置学校日**{ #configuring-school-days-under-general-details } 

创建学年后，管理员必须配置教学结构

1. 从列表中查找学年  
2. 点击查看详情

系统会打开**学年配置页面**，在这里可以配置其他元素，还可以在 "常规详情 "下配置学年。

![](SEMISconfigimages/image19.png)
![](SEMISconfigimages/image10.png)

管理员可以定义**正常学校工作日**。

通常包括

* 星期一  
* 星期二  
* 星期三  
* 星期四  
* 星期五

如果学校在周末开课，也可以启用这些日子。

![](SEMISconfigimages/image21.png)

### **配置学校条款**{ #configuring-school-terms } 

学期将一学年划分为不同的教学时段。

脚步

1. 在 "学年详情 "页面，找到 "**学期 "部分**。  
2. 点击 **添加术语**。  
3. 输入以下信息：

| The manual merge is suitable when there are resolvable conflicts or when not all the data needs to
be moved during the merge. For example, if an attribute has different values in both tracked
entities , the user can specify whether to keep the original value or move over the duplicate's
value. Since the manual merge involves the user explicitly requesting to move data, there are some
additional checks:  | Parameter name |
| :---- | :---- |
| 术语名称 | 学期名称（如第 1 学期、第 2 学期） |
| 开始日期 | 学期开始日期 |
| 结束日期 | 学期结束日期 |

4. 保存

**重要规则**

* 学期必须**在学年日期内**。  
* 条款 ** 不得重叠**。  
* 每个学年应包含**个必修学期**。

![](SEMISconfigimages/image8.png)

### ** 配置非上课日**{ #configuring-non-school-days } 

非上课日包括**节假日、公共假期和学校停课**。

### **步骤** {#steps}

1. 在**学年详情页面**，转到**非上课日**。  
2. 点击 **添加非上课日**。  
3. 输入所需详细信息：

| The manual merge is suitable when there are resolvable conflicts or when not all the data needs to
be moved during the merge. For example, if an attribute has different values in both tracked
entities , the user can specify whether to keep the original value or move over the duplicate's
value. Since the manual merge involves the user explicitly requesting to move data, there are some
additional checks: | Parameter name |
| :---- | :---- |
| query | 提及日期 |
| Potential duplicate status | 关闭原因（特别活动/公众假期） |
| 合并策略(DISCARD 或 LAST_UPDATED) | 活动名称 |

4. 单击保存。

在计算出勤率时，这些日期将自动排除在外。

![](SEMISconfigimages/image17.png)

# 使用日历{ #validation-checklist-before-using-the-calendar } 前的验证核对表 

在学校使用校历之前，管理人员应核实以下内容：

1. Academic Year Exists （存在学年）- 确保创建并激活**当前学年。  
2. 术语正确。

   请确认

* 术语不重叠  
* 学年内的学期

3. Holidays Are Configured （假期已配置）- 确认公共假期和学校停课已正确定义  
4. Operational Days Are Accurate （运行日准确）- 确保配置的学校日反映**实际的学校时间表**。  
5. Calendar Visibility \- 确保目标学校和用户**可以访问学年**。

# 学生配置概述{ #student-configuration-overview } 

SEMIS 应用程序中的 "学生配置 "部分允许管理员控制与学生相关的模块在系统中的运行方式。通过这个配置界面，管理员可以启用或禁用支持学生管理工作流程的特定模块。

这种配置有助于确保根据学校的运行需要，只向其提供所需的模块。

## **访问学生配置**{ #accessing-student-configuration } 

访问 "学生配置 "设置：

1. 在 DHIS2 中打开**SEMIS 应用程序**。  
2. 导航至 **配置**。  
3. 选择 **学生**。

本页显示一组模块图标，代表可配置的不同学生相关流程。

本节的主要目的是**定义和控制 SEMIS 系统中学生工作流程**的行为。

管理员可以使用此配置来

* 启用或禁用特定的学生管理模块。  
* 控制哪些功能对用户可见。  
* 根据机构要求定制系统。

这有助于简化工作流程，防止用户访问不需要的模块。

## **学生单元**{ #student-modules } 

学生配置界面包括几个支持学生生命周期的模块。

### ** 注册**{ #enrollment } 

该模块管理**学校的学生入学和注册**。它允许用户注册学生并记录其入学详情和社会经济详情。

1. 指定注册详细信息，例如年级、年级筛选器名称、注册计划阶段（已配置 DHIS2 跟踪器）等，以及社会经济详细信息  
2. 指定默认配置并提交。  
3. 填写所有必填字段，以 （*）标出

   ![](SEMISconfigimages/image9.png)

### ** 出席情况**{ #attendance } 

考勤模块使学校能够**记录和管理学生考勤**。出勤跟踪使用配置的校历和学年。

1. 选择并确认 DHIS2 追踪器配置的学生程序。  
2. 配置出勤状态详情和一般详情，如计划阶段、出勤状态（出席、缺席）  
3. 配置课堂考勤（可选）
![](SEMISconfigimages/image11.png)

### **性能**{ #performance } 

成绩模块用于**采集和管理学生的学业成绩**，如成绩、评估和评价结果。

1. 选择并确认 DHIS2 追踪器配置的学生程序  
2. 选择绩效计划阶段（已配置 DHIS2 追踪器）

![](SEMISconfigimages/image14.png)

### **转移**{ #transfer } 

该模块允许管理员在系统内**管理学校之间**的学生转学。

1. 选择并确认 DHIS2 追踪器配置的学生程序。   
2. 确认传输请求字段/状态映射   
3. 确认与配置状态相关的传入/传出列表行为  
4. 确认批准/拒绝流程字段。

![](SEMISconfigimages/image9.png)

### **最终结果**{ #final-result } 

最终结果模块管理**学业结束时学生的升级、结业或最终学业成绩**。

1. 配置计划详情（选择 DHIS2 追踪器配置的学生计划）。   
2. 在 "晋升与退学标准 "和 "退学状态 "下配置可晋升/可退学状态值。   
3. 配置最终结果详情和   
4. 点击提交

![](SEMISconfigimages/image7.png)

# 工作人员配置概览{ #staff-configuration-overview } 

SEMIS 应用程序中的**工作人员配置**部分允许管理员控制与工作人员相关的模块在系统中的运行方式。通过这个配置界面，管理员可以启用、禁用或调整不同的员工管理模块，以配合机构的工作流程。

这种配置可确保用户只能使用所需的教职员工管理功能，并有助于实现各学校教职员工相关流程的标准化。

## ** 获取工作人员配置**{ #accessing-staff-configuration } 

访问 "员工配置 "设置：

1. 在 DHIS2 中打开**SEMIS 应用程序**。  
2. 导航至 **配置**。  
3. 查看工作人员下的不同模块。

配置页面显示多个**模块图标**，代表系统中与工作人员相关的不同流程。

工作人员配置部分旨在**绘制和控制 SEMIS 应用**内的工作人员工作流程。

管理员可以使用该界面

* 配置如何管理员工记录。  
* 启用或禁用与工作人员相关的模块。  
* 控制员工工作流程功能的可见性。  
* 根据管理要求定制应用程序。

这有助于确保系统以结构化和受控的方式支持工作人员的生命周期。

工作人员配置界面包括多个模块，用于管理工作人员管理的不同方面。

![](SEMISconfigimages/image22.png)

### ** 工作人员登记处**{ #staff-registry } 

工作人员登记**模块允许管理员或授权用户在系统中**登记和维护工作人员记录**。  
 这包括工作人员的基本信息，如身份详情、就业信息和机构分配。

1. 选择并确认已配置 STAFF 程序的 DHIS2 追踪器。   
2. 指定注册详细信息，如员工类型、员工类型过滤器名称、注册计划阶段（已配置 DHIS2 跟踪器）等，以及社会经济详细信息。  
3. 指定默认配置并提交。   
4. 填写所有必填字段，以 （*）标出

![](SEMISconfigimages/image15.png)

### ** 出席情况**{ #attendance } 

通过**出勤模块**，学校可以**记录和监测教职员工的出勤情况。  
 它允许机构跟踪员工的日常出勤情况，并为行政目的保存出勤记录。

1. 选择并确认已配置 STAFF 程序的 DHIS2 追踪器。   
2. 配置出勤状态详情和一般详情，如计划阶段、出勤状态（出席、缺席）。   
3. 配置课堂考勤（可选）

![](SEMISconfigimages/image16.png)

### **转移**{ #transfer } 

通过**调动模块**，管理员可以**管理学校或机构之间的人员调动**。  
 这可确保在员工更换工作地点时，员工记录保持更新。

1. 选择并确认已配置 STAFF 程序的 DHIS2 追踪器。   
2. 确认传输请求字段/状态映射   
3. 确认与配置状态相关的传入/传出列表行为。  
4. 确认批准/拒绝流程字段

![](SEMISconfigimages/image5.png)

### **重新注册**{ #re-enroll } 

重新注册模块**允许管理员在必要时**重新激活或重新注册工作人员记录**。  
 当工作人员中断服务后返回或需要在系统内重新分配工作时，可使用此功能。

1. 选择并确认已配置 STAFF 程序的 DHIS2 追踪器。  
2. 在 "升级与退学标准 "和 "退学状态 "下配置可重新注册的状态值。  
3. 将最终结果详情配置为最终状态，然后点击提交。

![](SEMISconfigimages/image12.png)

# 交叉配置依赖关系{ #cross-cutting-config-dependencies } 

SEMIS 中的多个配置组件相互关联。一个区域的配置更改或不完整可能会影响整个系统中多个模块的行为。因此，管理员应确保在启用相关工作流程之前正确完成基础配置。

了解这些依赖关系有助于防止系统错误、不完整的数据输入流程和不正确的模块行为。

## **校历依赖性**{ #school-calendar-dependency } 

在使用**考勤**等模块之前，必须正确定义**校历配置**。

日历规定

* 学年**  
* **学期**  
* **学校工作日**  
* **非上课日或节假日**

考勤记录依靠此配置来确定有效的考勤日期。如果校历配置不当：

* 可能无法提供考勤记录。  
* 某些日期可能无法参加会议。  
* 报告可能会显示不正确的出勤率计算。

因此，在测试或启用考勤模块之前，管理员应确认学年、学期和上课日已配置。

## ** 注册映射依赖**{ #enrollment-mapping-dependency } 

注册配置**定义了学生在系统中注册和映射的方式。

该配置直接影响

* **学生搜索功能**  
* **不同模块的学生名单**  
* 更新**学生档案**  
* **跨模块的工作流程导航**

如果注册映射不完整或配置不正确，用户可能会遇到以下问题：

* 学生未出现在搜索结果中  
* 考勤或成绩模块中的学生记录缺失  
* 更新学生信息时出现错误

确保正确的注册配置可保证学生记录在所有与学生相关的模块中正确流动。

## **最终结果配置依赖性**{ #final-result-configuration-dependency } 

最终结果配置**决定了系统如何处理学生的年终学习成绩。

该配置会影响以下行为

* **推广过程**  
* **完成情况**  
* **最后学业记录**

具体来说，配置控制着系统界面中**最终结果操作按钮**的启用或禁用。

如果该配置缺失或不正确：

* 最终结果模块可能不可用。  
* 结果提交按钮可能仍然禁用。  
* 学生不得升入下一学级。

管理员必须确保在年终处理开始前正确配置最终结果状态和相关设置。

## **转移配置依赖性**{ #transfer-configuration-dependency } 

转学配置**管理学校或机构之间如何处理学生或教职员工的转学。

该配置定义

* 转账申请工作流程  
* 审批要求  
* 机构间的记录流动

如果传输配置不完整或禁用：

* 可能无法生成转账请求。  
* 审批工作流程可能无法正常运行。  
* 学生或教职员工的记录可与原机构保持联系。

正确的转移配置可确保个人在更换机构时，记录能在 SEMIS 系统内准确转移。

