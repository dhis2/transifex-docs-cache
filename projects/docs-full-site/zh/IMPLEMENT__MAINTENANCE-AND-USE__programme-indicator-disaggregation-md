---
edit_url: "https://github.com/dhis2/dhis2-docs-implementation/blob/master/content/maintenance_use/program_indicator_disaggregation.md"
revision_date: '2025-11-19'
tags:
- 实施
---

## 概念{ #the-concept } 

在 DHIS2 v42 中，现在增加了将分类类别组合分配给计划指标的功能，并在计划数据与组合中包含的每个类别选项之间创建映射。这就在跟踪数据模型和汇总数据模型之间建立了一种关系，从而能够以相同的方式分析单个数据和汇总数据。

节目链接到类别](resources/images/Program_to_category.png){ .center width=30% } !
跟踪器和聚合模型](resources/images/Tracker_to_aggregate_model.png){ .center width=30% } !

在计划层面定义的计划指标分类映射提供了 DHIS2 中两个数据模型之间的连接。这最终允许用户在数据展示台中使用单一计划指标创建计划数据的分类视图，而以前需要为每个分类创建一个计划指标。

![表格示例](resources/images/Table_Example.png){ .center width=30% }

如何配置计划指标分类，请参阅[用户指南]（#program_disaggregation_mapping）。
(#program_disaggregation_mapping)

