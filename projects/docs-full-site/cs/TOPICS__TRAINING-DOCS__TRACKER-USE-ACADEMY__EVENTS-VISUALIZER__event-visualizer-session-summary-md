---
edit_url: "https://github.com/dhis2/training-docs/blob/main/content/tracker_use/summary_event_visualizer.md"
revision_date: '2024-07-08'
tags:
- Školicí dokumenty
---

# Event Reports - Session Summary { #event-reports-session-summary } 

The event visualizer app allows you to create charts based on tracker data. It is particularly well suited for visualizing standardized responses (ie. option set information).

Při vytváření vizualizace, která používá jako typ výstupu událost, se s daty události a sledovače zachází stejným způsobem, protože můžete vybrat pouze jednu fázi programu, se kterou chcete pracovat v rámci vizualizéru událostí. Typ výstupu „registrace“ v této aplikaci nefunguje.

Ve vizualizéru událostí existuje typ výstupu „instance trasované entity“. To bude fungovat pouze s daty trasovače (nikoli daty událostí), protože počítá počet jedinečných instancí trasovaných entit, které splňují daná kritéria v rámci jednoho programu.

## Creating an event visualization { #creating-an-event-visualization } 

1. Vyberte typ grafu
2. Vyberte svůj program a fázi programu
3. Vyberte svá data
4. Definujte filtry pro vybraná data
5. Vyberte období
6. Vyberte organizační jednotku
7. Upravte rozvržení
8. Aktualizujte svou vizualizaci

By default, an event visualization will use event as its output type. This means these visualizations are summarizing **all events** within a ***single*** program stage, including any repeated events within the stage you have selected.

## Tracked entity output type { #tracked-entity-output-type } 

We can modify our charts to use the output type "Tracked entity instance." In order to do this, we select the chart options menu and modify the output type. This will count the number of TEI's that meet your criteria. Repeated event data would therefore be excluded from this output. 

Na rozdíl od výstupů typu zápis však nejsme schopni prohlížet data z více fází při použití typu výstupu TEI ve vizualizaci událostí.

Všimněte si, že i když v tomto seznamu typů výstupu existuje zápis, nebude fungovat a měl by být ignorován.

## Enrollment outputs vs. tracked entity instance outputs { #enrollment-outputs-vs-tracked-entity-instance-outputs } 

Proč rozdílný jazyk mezi reporty událostí a vizualizérem událostí, pokud jde o typ výstupu? Výstupní typ zápisu v sestavách událostí vám umožňuje zobrazit informace jedinečné pro zápis a také získat data v rámci celého zápisu (to znamená, že můžete prohlížet data z více fází programu společně v seznamu řádků; a získávat jedinečné počty prostřednictvím kontingenční tabulky ). Typ výstupu "Instance trasované entity" ve vizualizéru událostí je mnohem více omezující. Nedovolí vám tahat data z více fází programu; stačí vám spočítat počet jedinečných trasovaných entit (obvykle lidí), které splňují kritéria, která jste nastavili pomocí filtrů v rámci fáze programu, 1 nebo vícekrát. To může být užitečné, ale je důležité tento rozdíl identifikovat.

## Saving an event visualization { #saving-an-event-visualization } 

1. Vyberte nabídku oblíbených položek
2. Vyberte "Uložit jako"
3. Zadejte svůj přehled s názvem a uložte jej

## Downloading an event visualization { #downloading-an-event-visualization } 

1. Vyberte nabídku stahování
2. Vyberte formát, ve kterém chcete zprávu stáhnout
3. Otevřete stažený přehled jeho načtením ze složky stažených souborů

